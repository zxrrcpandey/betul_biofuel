{
  "name": "bbpl-approvals-frontend",
  "private": true,
  "version": "2.31.0",
  "description": "BBPL Approvals executive PWA — Vite build workspace. NEVER deployed to a server; built dist is committed under trustbit_ethanol/public/exec/. This file must stay in frontend/ — a package.json with a build script at the APP ROOT would make bench build run yarn there and abort production deploys.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/postbuild.mjs"
  },
  "dependencies": {
    "frappe-ui": "0.1.278",
    "vue": "^3.5.41",
    "vue-router": "^4.4.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.14",
    "vite": "^5.4.21"
  }
}

# Trustbit Ethanol — TS Gate Entry System

**Version:** 2.21.2 | **ERPNext:** V15 | **Module:** TS Gate Entry

Custom ERPNext v15 app for **Betul Bio Fuel Pvt. Ltd.** — an ethanol manufacturing plant. Handles the complete vehicle gate-to-exit lifecycle, multi-level PO/MR approval with CC-based routing, budget management, item creation, quality inspection, and interactive dashboards, using a **sealed (weight-blind) gate-pass system** designed to prevent data manipulation.

## Recent ships (May–Jul 2026)

> ⚠ **Prod note:** production (`erpbbpl.com`) runs a **prod-local 2.18.x hotfix line** (selective deploys, no `git pull`) that diverged from develop after v2.18.9 — develop v2.19.x/v2.20.0 content maps to prod v2.18.10–.19, so develop version numbers ≠ prod version numbers. Rows below are the **develop** line; ⇄ marks the matching prod-local version where one was deployed.

| Version | Date | Change |
|---|---|---|
| **2.21.2** | 8 Jul | **Docs-only — README changelog backfill (v2.17.1–v2.21.0) + prod-divergence note + version-header sync (no code / behavior / schema change; no deploy needed).** Backfilled eight grouped changelog rows for the previously-missing v2.17.1–v2.21.0 run (release chains grouped into single rows, sourced from `git log` + the memory changelog), added the prod-divergence note above the table (prod runs a prod-local 2.18.x hotfix line; develop v2.19.x/v2.20.0 content ⇄ prod v2.18.10–.19, so version numbers differ), and synced the version header 2.17.0 → 2.21.2. Prose in `README.md` only; app code deliberately untouched. security **CLEAN (0 findings, APPROVE)** — verified the security-relevant row claims against actual code (PQC + `has_permission` pair, raw-SQL leak plugs, self-healing backfill wiring), no IPs / credentials / confidential identifiers leaked, kill-switch mentions match existing precedent; demo regression **22/22 stands** (v2.21.0 `47441c7`; this diff touches zero runtime code). `README.md` + `__init__.py` bump only. **No deploy required (docs-only).** |
| **2.21.1** | 8 Jul | **Docs-only — README vocabulary reword (no code / behavior / schema change; no deploy needed).** Reworded the identity line (“blind token-based system … prevent manipulation” → “sealed (weight-blind) gate-pass system … prevent data manipulation”) and Key Feature #1 (“Token Lifecycle” → “Gate-Pass Workflow (Token)”) for clearer plant-domain wording — same mechanism (the gate pass is weight-blind: the operator never sees the expected weight). Prose in `README.md` only; app code deliberately untouched. Wraps the doc change already committed in `a50f793` into a versioned ship for the changelog record. security **CLEAN (0C/0H/0M/0L)** on the `a50f793` diff (only `README.md` staged — no foreign WIP); demo regression **22/22 unaffected** (v2.21.0 `47441c7`; zero runtime code touched). `README.md` + `__init__.py` bump only. **No deploy required (docs-only).** |
| **2.21.0** | 8 Jul | **Department Actual Production gate + department status dashboards + premium page de-clutter** (`47441c7`). Department BOM lines now GATE the Multiple flow: PM submit auto-creates Pending department entries + notifies; auto-MR + Store-Manager-notify deferred to an atomic all-complete trigger; Administrator-only skip; department status board for PM/Grain PM/CEO/MD/SM. Regression suite extended to **22/22** (17 core + 5 env-aware production smoke). Kill-switched — demo flags ON for UAT, prod OFF. |
| **2.20.1–2.20.6** | 2–3 Jul | **Production Multi-BOM flow — all 4 phases + UAT refinements.** Release rename + config masters + notify engine (2.20.1) · department consumption entries, reporting-only (2.20.2) · Multiple (Connector) flow + Production Cascade Delete (2.20.3) · TS Production workspace shortcuts (2.20.4) · Store Manager = Release ONLY, reject admin-gated server-side (2.20.5) · deep-audit remediation: auto-MR sources STORES not the WIP artifact, cascade hard-blocks Multiple runs, dept list includes Awaiting Distribution, form Reject admin-gated (2.20.6). All kill-switched (demo ON for UAT; prod OFF/absent). |
| **2.20.0** | 2 Jul | **Maize PO row-level confidentiality + Quality Reports DocPerm restriction + maize LINE-ITEM detection fix** (`8ca77b7`) ⇄ prod v2.18.16 + v2.18.19. The app's first `permission_query_conditions` + `has_permission` pair: `ts_confidential` auto-set on maize POs hides PO/PR/PI/MR rows from unauthorized roles, raw-SQL read paths plugged; header-OR-line-item maize detection with a self-healing, never-clear backfill on every after_migrate. Locks added: MAIZE_PO_CONFIDENTIAL + QUALITY_INSPECTION_PERMS. |
| **2.19.7–2.19.8** | 1 Jul | **Material Issue "Cost Center (all items)"** ⇄ prod v2.18.17/.18. Header cost centre now FORCE-applies to all rows (the old fill-blank cascade let ERPNext's row default win, mis-attributing reports to company-default "Main - BBPL") and is REQUIRED at submit. Historical mis-bookings re-attributed via direct-SQL dimension change (Dr=Cr preserved). |
| **2.19.1–2.19.6** | 29 Jun–1 Jul | **PO purchasing chain** ⇄ prod v2.18.10–.14. GST Breakdown table (CGST/SGST/IGST + Total) on form + BBPL printout (2.19.1) · Item Creator approver-edit + approval notifications; PO-list column lock (2.19.2) · PO 🚚 Deliveries button + dialog replacing the leaky inline delivery chain, Lesson 296 (2.19.3) · creator-level PO budget + MR-variance warnings at save & submit (2.19.4) · GST Due Date + "Including GST" drives Payment Amount, net↔with-GST (2.19.5) · JS cache-bust chore (2.19.6). |
| **2.19.0** | 26 Jun | **Production Logging + OMC Supply Tracker dashboards** (`c378b95`) — workspaces + animated gauges. **DEVELOP-ONLY**, behind `ts_production_entry_enabled=0`; deliberately NOT deployed to prod (this is where the prod-local 2.18.x line diverged). |
| **2.18.0–2.18.9** | 20–25 Jun | **WhatsApp Integration Phase 1 (Airtel IQ)** — foundation (2.18.0) · instant approval nudges + escalation scheduler (2.18.1) · conditionally-mandatory credential fields (2.18.2) · multi-adapter, WhatsHub360 + Airtel IQ (2.18.3) · WhatsApp Number in User Management (2.18.4) · dedicated TS WhatsApp doctypes + quick-reply buttons + auto delivery-status (2.18.5) · self-service variable recipes + read-only Preview (2.18.8) · Item Creator + Cascade Delete notifications (2.18.9). LIVE on prod but **doubly inert** (`ts_whatsapp_enabled=0` + no prod Airtel creds). Also: G2 Gate dashboard "today" scoping + source-of-truth number cards (2.18.6/.7). |
| **2.17.1–2.17.7** | 4–20 Jun | **Prod maintenance chain** (LIVE on prod): SE Material Issue mandatory fields + header CC/Project cascade (2.17.1) · TS Material Inspection naming BBF→BBPL, Lesson 291 (2.17.2) · Stock Reconciliation IT-Head-only DocPerm, Lesson 292 (2.17.3) · Item Creator FA-prefix + Reject-button fixes, Lessons 294/295 (2.17.4) · Material Issue SE CC/Project propagation + MR Transfer, Lesson 288 (2.17.5/.6) · TS Material Issue Ledger "Consolidated (by Cost Center)" report type (2.17.7). |
| **2.17.0** | 4 Jun | **Item Creator approval flow — any desk user submits an item-creation request; Stores/IT Head approve.** Splits the old one-click create into **request → approve**. A non-floor desk user fills the Item Creator page → button reads **"Submit for Approval"** → `submit_item_request` (module `@frappe.whitelist(methods=["POST"])`, **denylist-gated** so pure G1/G2/Weighbridge operators + Guest are blocked) inserts a **`Pending Approval`** record with **no Item created**. An approver (**Stores User / Stores Manager / IT Head / System Manager**) sees **Approve / Reject** buttons on the TS Item Creator form (Reject requires a reason) — Approve calls the privileged `create_item`, Reject sets `Rejected` + reason. **Approvers keep the one-click self-create.** Server (`ts_item_creator.py`): `create_item` is now **POST-only + `_is_item_approver`-gated** (Lesson 175 + 224 — the privileged ERPNext Item insert); new `approve_request` / `reject_request` (instance, POST); **`_block_item_field_tampering`** `before_save` guard on `requested_by`/`approved_by`/`approved_on`/`rejection_reason`/`status`/`item_created` (Lesson 162, bypass via `frappe.flags.in_item_request` try/finally — Lesson 176); `is_current_user_item_approver` + `get_my_recent_requests` read-only getters so the broadened page audience needs no read-DocPerm (Lesson 168). DocType: status enum **`Draft → Pending Approval → Created / Rejected`** (no Property Setter — applies via reload-doc/migrate), 4 new read-only fields, **+Stores Manager DocPerm** (also in `seed_data` CUSTOM_DOCPERM). Page (`item_creator.js/.json/.css`): submit-vs-create branch on an approver flag, amber **pending** success state (no Item link), recent list via the new getter, **page roles broadened to ~30 desk roles** (floor ops excluded). guardian GO (only `seed_data.py` needed the MR_FULL/PO_FULL file-level unlock for a 1-row CUSTOM_DOCPERM add — Property Setters byte-untouched — re-locked); security **CLEAN (0 findings)**; ui-designer 1 HIGH fixed (amber pending icon); code-tester **90/90**; live-data **26 approvers / 21 requesters / 3 floor-ops denied** on real users; demo regression **17/17** before+after. 7 source files + `__init__.py` + README. ⚠️ Prod deploy: `reload-doc` doctype+page, run `seed_custom_docperm`, **apply page roles via ORM** (migrate doesn't sync page roles — like Lesson 173), `__pycache__` clear + `bench build` + `clear-website-cache`. No `hooks.py` change → no Lesson-252 cache clear. |
| **2.16.4** | 4 Jun | **Two bundled Purchase Receipt enhancements — (1) Supplier Invoice No + Date now MANDATORY at SUBMIT, and (2) read-only Vehicle Number field auto-fetched from the linked Gate Token, with both vehicle fields backfilled server-side.** **(1) Supplier invoice required on submit:** NEW `ts_pr_bill_check.require_supplier_bill_on_submit` (PR `before_submit`) blocks submitting a Purchase Receipt unless **Supplier Invoice No (`bill_no`) + Supplier Invoice Date (`bill_date`)** are both filled (static throw message naming the missing field(s)). Enforced at **SUBMIT, not on save**, so the Stores Receiving Dashboard / Token GRN can still create DRAFT PRs that legitimately have no supplier invoice yet at goods-receipt time — **GRN flows unaffected**. **(2) Vehicle Number on PR:** NEW `ts_vehicle_origin_propagation.seed_pr_vehicle_number_field` (`after_migrate` seeder) adds a **read-only "Vehicle Number" Custom Field** on Purchase Receipt placed **`insert_after` `vehicle_origin`**, `fetch_from` `ts_token.vehicle_number` (idempotent — no-ops if it already exists). Because `fetch_from` is **client-only** (doesn't fire on the GRN "Get Items" mapper or programmatic PRs), NEW `pr_backfill_vehicle` (PR `before_validate`) fills **`vehicle_number` AND `vehicle_origin`** from the linked TS Token **only when EMPTY** (parameterized `frappe.db.get_value`, **never overwrites**, no-token safe). `hooks.py`: PR `before_validate` is now a **LIST** (`ts_use_location_propagation.purchase_receipt_backfill` + `ts_vehicle_origin_propagation.pr_backfill_vehicle`), added `before_submit → require_supplier_bill_on_submit`, and `after_migrate += seed_pr_vehicle_number_field` (**all existing handlers preserved**). **One new read-only Custom Field via seeder; no new whitelist endpoint; no qty / rate / accounting touched.** guardian GO (`setup.py` avoided as MR_FULL-locked → vehicle field added in `ts_vehicle_origin_propagation.py` instead; `hooks.py` / `ts_pr_bill_check.py` / `ts_vehicle_origin_propagation.py` not locked); security **CLEAN (0 findings)** — parameterized, no `@whitelist`, fill-when-empty, no tamper-guard conflict; functional on demo — **bill:** draft saves without invoice / submit blocked when empty / passes when set; **vehicle:** field after `vehicle_origin` + read-only, `vehicle_number`+`vehicle_origin` backfill from token, never-overwrite, no-token safe; demo regression **17/17**. 3 source files (`ts_pr_bill_check.py`, `ts_vehicle_origin_propagation.py`, `hooks.py`) + `__init__.py` bump + README row. Commit + push only — orchestrator handles the prod deploy (⚠️ `hooks.py` changed → run `bench migrate` for the new Custom Field seeder, then clear `hooks` / `app_hooks` Redis cache keys after the supervisor restart, Lesson 252). |
| **2.16.3** | 4 Jun | **Define Use Location + Item Remark now propagate down the full MR→PO→PR→PI chain (server-side `before_validate` backfill closing the PO→PR gap).** ERPNext's `fetch_from` is **client-only** — it does NOT fire on the "Get Items" mapper or any programmatic/server-side insert, and `fetch_if_empty` does not help server-side either (both verified empirically) — so the **PO→PR hop dropped `ts_delivery_location` ("Define Use Location") and `ts_item_remark`**, breaking the chain mid-way. NEW `ts_use_location_propagation.py` adds two `before_validate` handlers — `purchase_receipt_backfill` (PR Item ← PO Item via `purchase_order_item`) and `purchase_invoice_backfill` (PI Item ← PO Item via `po_detail`, fallback PR Item via `pr_detail` for PI-from-PR) — that **backfill the two fields from the linked upstream line ONLY WHEN EMPTY** (batched `frappe.get_all`, **in-memory only**, idempotent, **never overwrites** an existing value). `hooks.py` merges `before_validate` into the existing **Purchase Receipt** + **Purchase Invoice** `doc_events` dicts (all existing handlers preserved). Paths that already set the fields (Stores Dashboard, Token GRN) no-op here. **No new DocType / schema / migration / whitelist endpoint; read-only fill — no qty / rate / accounting touched.** guardian GO (PO flow untouched; `hooks.py` / PR / PI not locked); predictor LOW–MEDIUM, no financial impact, rollback easy; security **CLEAN (0 findings)** — no SQLi, no `@whitelist`, no tamper-guard conflict, in-memory fill-when-empty; functional chain test on demo **PO→PR ✓, PO→PI ✓, never-overwrite ✓** (rolled back); demo regression **17/17**. 2 source files (`ts_use_location_propagation.py` NEW, `hooks.py`) + `__init__.py` bump + README row. Commit + push only — orchestrator handles the prod deploy (⚠️ `hooks.py` changed → clear `hooks` / `app_hooks` Redis cache keys after the supervisor restart, Lesson 252). |
| **2.16.2** | 4 Jun | **Material Transfer / Material Issue Stores-Manager flow — "Option A": the Stores Manager can edit item quantities BEFORE approving; submit-on-approve; Revise & Resubmit; ledger keeps pending visible.** Reported gap: when a user created a **Material Transfer / Material Issue** MR and forwarded it to the store, the Stores Manager could NOT adjust items or quantities because the forward step submitted the MR (docstatus 0→1) and the item-lock guard then froze the grid. **This is a DELIBERATE forward-change to the MR docstatus lifecycle (user-approved "Option A") — NOT a parity revert; a future Lesson-210 parity read must NOT undo it.** `submit_for_stores_approval` is now **SAVE-ONLY**: the MR stays a **DRAFT (docstatus=0)** at `Pending Stores Manager` so the Stores Manager can edit quantities (a legacy already-submitted doc just updates the status field in place, back-compat). `approve_transfer` now **submits the draft atomically (validate → save → submit → reload) BEFORE `make_stock_entry`** (Lesson 288 — ERPNext's MR→SE mapper needs docstatus=1; valuation is still hard-blocked first, then re-validated since the SM may have changed qty). `handle_before_save` item-lock **relaxed**: `Approved` stays HARD-locked (a Stock Entry already exists — editing would desync it), `Pending Stores Manager` is editable ONLY by Stores Manager / System Manager / Administrator / the workflow endpoints (everyone else, incl. the creator, is blocked). NEW **`revise_and_resubmit_transfer`** endpoint (`@frappe.whitelist(methods=["POST"])`, **creator-only**) moves a **Rejected DRAFT → Not Submitted** so the creator can edit and resubmit the SAME MR (native Amend doesn't apply to a draft; history kept); `ALLOWED_TRANSITIONS` gains `Rejected → {Not Submitted}`. `get_transfer_context` exposes **`can_edit_items`** (true only when `Pending Stores Manager` + SM/admin + docstatus=0) and its Stock Entry lookup now covers **Material Issue** as well as Material Transfer. **Client (`mr_approval.js`):** the items grid is editable only when `can_edit_items` (SM sees an "adjust quantities then Approve" banner) and read-only + `disable_save()` for everyone else; **Approve persists the SM's unsaved qty edits FIRST** (`frm.save()` before the call, since `approve_transfer` reloads from the DB); the Revise & Resubmit action is wired. **Ledger (`ts_material_issue_ledger.py`):** Pass A `WHERE` widened from `docstatus=1` to `docstatus=1 OR (docstatus=0 AND ts_mr_status='Pending Stores Manager')` so forwarded-pending drafts stay visible. **No new DocType / schema / migration; no new DocPerm (Stores Manager already has MR write).** Gates ALL GREEN on demo: guardian (MR_FULL unlocked for the build, re-locked post-deploy); security **CLEAN (0 findings)**; code-tester **529/529 PASS** + regression **17/17 BEFORE and AFTER**; live-data-tester financial-critical paths PASS (zero-valuation hard-block holds, SE correctness, ledger visibility); demo left clean. 3 source files (`ts_mr_transfer.py`, `mr_approval.js`, `ts_material_issue_ledger.py`) + `__init__.py` bump + README row. Commit + push only — orchestrator handles the prod deploy. |
| **2.16.1** | 4 Jun | **TS Material Issue Ledger — Material Request Type + Stock Entry Type filters (with MR·/SE· indicators).** Two new Select filters on the report: **Material Request Type** and **Stock Entry Type**, each `All / Material Issue / Material Transfer`. Display labels are prefixed `MR ·` / `SE ·` so the two otherwise-identical dropdowns are instantly distinguishable; the **bound value stays the raw type** (`All` / `Material Issue` / `Material Transfer`) so the query is unchanged. **Server narrowing** is fully parameterized (`%(eff_type)s` / `%(se_type)s`, never interpolated): in `_query_mr_based` (MR-based pass — a request's movement type equals its request type) BOTH filters constrain `mr.material_request_type` (**MR Type wins if both are set**) and Stock Entry Type also narrows the SE-purpose subqueries; in `_query_standalone_se` (Pass B) a **specific Material Request Type excludes standalone SEs** (no MR origin) and Stock Entry Type narrows `se.purpose`. **Default `All` = byte-identical to prior output;** the branded-PDF + Excel exports inherit the filters (they call the report's `get_data`). **No new endpoint / DocType / schema / migration; read-only report change.** guardian GO (report files unlocked-by-design); security CLEAN (0 findings — parameterized SQL, read-only, no new sink); functional tests on demo **8/8 + 6/6 + 6/6** (filter matching, MR-type excludes standalone, SE-type keeps standalone-of-type, All==default, garbage/injection → 0 rows; real data: All 81 / MR=Issue 30 / MR=Transfer 29 / SE=Issue 47 / SE=Transfer 34 → 47+34=81 clean partition); demo regression **17/17**. 2 report files (`.js`, `.py`) + `__init__.py` bump + README row. Commit + push only — orchestrator handles the prod deploy. |
| **2.16.0** | 4 Jun | **Token Search in the Stores Receiving Dashboard — live in-page filter + any-token server lookup; plus a Section A/B dead-button fix.** New read-only `search_token(query)` whitelisted endpoint (gated by `_check_read()`, per-candidate `frappe.has_permission("TS Token","read")` so it never discloses a token the caller can't read, **parameterized LIKE** with `%`/`_` wildcards escaped, capped at 25 results) — finds ANY token by number / vehicle / supplier / PO across all states. The dashboard JS first filters the already-loaded rows in-page (zero round-trips via a `data-search` key per row); only when nothing matches does it call the server for a fallback **result card** that classifies receivability (`_classify_receivable` MIRRORS the `create_grn_*` POST guards — Path A weighbridge-complete / Path B Non-RM no-weighing) and reuses the existing `create_grn_for_weighed_token` / `create_grn_for_non_weighing_token` POST endpoints (Section B handler extracted to reusable `_sr_section_b_start`). **Dead-button fix:** tightened `_fetch_section_a_tokens` (require a submitted Gate Entry **and** a header PO) and `_fetch_section_b_non_weighing` (require a PO) so the section lists never render a "Create GRN" button the POST endpoint would immediately reject — debris tokens stay discoverable via the search lookup, which now shows the blocking reason. `SR_VERSION` bumped to `v6.2-2026-06-04-token-search`. **No new DocType / schema / migration; read-only endpoint (no mutation).** guardian GO (both files unlocked-by-design); security CLEAN (0 findings — feature scan + fix re-scan); code-tester **30/30**; live-data-tester **11/11** (role-scoping, receivability, injection, result cap); demo regression **17/17 BEFORE + AFTER**; dead-button fix verified on demo (Section A 37→35, B 2→1, 0 list-vs-action mismatches). 2 files (`stores_receiving_api.py` +181, `stores_receiving.js`) + `__init__.py` bump + README row. Commit + push only — orchestrator handles the prod deploy. |
| **2.15.1** | 3 Jun | **Workspace fixtures sync — version-controlled the BBPL Ethanol workspace family design from production (was DB-only / stale fixtures).** Production's polished **BBPL Ethanol** workspace family (gradient banner + Quick-Action tiles + Reports / Master-Data cards) was hand-built in prod's DB and **never exported to git** — the committed workspace fixtures still carried the old plain design. Regenerated the **7 stale fixtures** (`bbpl_ethanol`, `item_management`, `admin_reception`, `management_dashboard`, `weighbridge`, `g2_gate_operations`, `g1_security_gate`) to match prod's LIVE design, captured via Frappe's canonical `export_to_files` on demo (which mirrors prod). **Only design fields changed:** `content`, `number_cards`, `shortcuts`, `links`, plus a `parent_page` correction ("Trustbit Ethanol" → "BBPL Ethanol") and `title`. The **`roles` child tables and timestamps were deliberately PRESERVED** from the committed files (NOT widened) to avoid migrate-failure risk (prod references a `CTO` role absent from the app's Role fixtures). Protects prod against a workspace re-sync / DB-loss reverting to the old look, and makes fresh installs reproduce the design. **Fixtures-only — no Python / logic / schema / whitelist change; NO server deploy** (the DBs already have the design). guardian GO (no locked files; Admin Reception seeder stays idempotent on prod-sourced content); security CLEAN (0 findings — static presentational HTML, relative `/app` links only, roles byte-identical); demo regression **17/17**. All 7 files validated as well-formed JSON (embedded `content` also parses as valid JSON). 7 workspace JSON fixtures + `__init__.py` version bump + README row. Commit + push only. |
| **2.15.0** | 2 Jun | **TS Production Entry — Manufacturing BOM variance + CEO approval + auto by-product Stock Entry.** New feature: capture actual material consumed + produced qty against a **Standard BOM** → compute material & produced **variance %** → a tolerance breach routes to **CEO approval**; on approval (or auto when within tolerance) it creates+submits a native **Manufacture Stock Entry** (finished good + by-products DDGS/DWGS/LCO2 as auto `is_scrap_item` rows). **Data-driven across all BOMs** (by-products read from BOM Scrap Items — no hardcoded list). **3 new doctypes** (TS Production Entry header + TS Production Material + TS Production By-Product children) + `ts_production_api.py` (variance engine, atomic `_post_production`, valuation hard-block, hand-built Manufacture SE, submit/approve/reject/revise endpoints, Stock Entry `on_cancel` reverse-sync). **Additive-only** edits to non-locked files (hooks.py Stock Entry `on_cancel`→list, ts_settings.json +5 fields permlevel 1, ts_my_approvals_api.py CEO inbox map, bbpl_ethanol workspace link). **Ships OFF** (kill switch `ts_production_entry_enabled`) — inert on prod until the team creates DWGS/LCO2 items, sets the 3 warehouses + tolerance, and enables it. guardian CLEAR (verify-locks 14/14, **no unlock** — `ts_po_approval.py` byte-unchanged, helper only imported). **Phase 4 ALL GREEN (demo):** predictor HIGH/proceed; security **0C/0H/0M/0L** (re-scanned post audit-fix incl. new `revise_production`, runtime-probed); code-tester **136/136**; live-data-tester real-BOM auto-post + real CEO approve + SoD + **L227 migrate-survival**; regression **17/17 BEFORE+AFTER**. Pre-deploy audit round fixed 8 findings (save-before-submit, workspace link, revise-from-Rejected, BOM-coverage warning, multi-batch `standard_batches`, warehouse-company guard, snapshot re-check, posting_time). 16 files, +1668. **SHIPPED TO PRODUCTION 2 Jun 2026** (commit `99d38a4`; prod FF `6fd1942→99d38a4`; backup `20260602_205032`; `bench migrate` created 3 doctypes + 5 fields; build + `__pycache__` clear + clear-website-cache + restart + **hooks-cache clear L252**). **Post-deploy audit GREENLIGHT 0 issues** (config safe, ships OFF, Stock Entry `on_cancel` keeps all 3 handlers, `ts_po_approval.py` byte-unchanged, MR tabSeries intact, prod==GitHub, **prod regression 17/17**). |
| `6fd1942` (no version bump) | 2 Jun | **PO Print Format — removed the auto "Override (default X Per Qtl)" prefix** from the BBPL Purchase Order **Deduction Terms → Reason/Note** cell. Client wanted only the operator's note shown (the effective overridden rate is already in the Rate/Unit columns). Sole source confirmed: print-time Jinja in `bbpl_purchase_order.json` — no server/JS code writes it; data keeps default_rate/override_rate/override_reason as separate fields; the other PO formats have no Deduction block. Display-only 1-line change → `{{ od.override_reason or '' }}`. guardian CLEAR (this format is **NOT** locked — PO_PRINT_BUTTON protects the *other* "TS Purchase Order"/"…(Clean)" formats); lightweight track; regression 17/17. **Non-standard print format** (`standard:"No"`) so the deploy = commit + `git pull` (source parity) **plus** a direct `tabPrint Format.html` DB update (targeted exact-once replace) + clear-website-cache — **no migrate/build/restart**. **SHIPPED TO PRODUCTION 2 Jun 2026** (commit `6fd1942`; prod FF `90e9260→6fd1942`; backup `20260602_133644`). Verified on the reported PO `BBPL-PO-OT-2026-00296` (Brokerage 10→5): prefix gone, "Brokerage Charges Rs. 50/- per Mts." retained; prod regression 17/17. |
| **2.14.3** | 31 May | **Stores Receiving Dashboard — PARTIAL GRN receipt.** The weighed-token GRN dialog previously required EVERY item to have qty > 0, blocking the GRN whenever a truck delivered only some of a multi-item PO (reported: token `BBPL-TKN-2026-01736`, Aggregate items; the greyed ordered-qty placeholder also looked like a filled value). Fix (user chose "allow partial receipt"): `create_grn_for_weighed_token` now builds PR lines only for RECEIVED items — KG items always (weighbridge), non-KG items only when a manual qty > 0 was entered; **blank/0 non-KG rows are SKIPPED** (no PR line; stay open on the PO), the weighbridge weight is distributed over **received items only** (prevents KG mass-loss on mixed partials), and **≥1** received item is required. Client dialog (`stores_receiving.js`) skips blank/0 rows + requires ≥1, with a fixed placeholder + "leave blank to skip" note. **No stranding** — a PO can ride MULTIPLE trucks/tokens until 100% received (`ts_gate_entry.py:292`), so skipped items arrive on a future token; the draft PR is reviewable and cancelling re-opens the token. Locked `TSToken.create_grn()` byte-untouched. **Phase 4 ALL GREEN (demo):** predictor CRITICAL→resolved (no stranding, received-only denominator); guardian CLEAR (not locked); security **COMMIT 0C/0H/0M/0L** (POST-only + role-gate + no-injection + no off-token PR fabrication intact); code-tester **15/15** real partial PRs + full cleanup; live-data-tester real multi-item scenarios, demo left clean; demo regression **17/17**. 3 files. **Shipped to production on the user's "deploy to production".** |
| **2.14.2** | 30 May | **TS Item - Warehouse Wise — TREE layout now always shows the warehouse leaf under each item.** Single-warehouse items (e.g. an item held only in `Stores - BBPL`) previously showed the item roll-up but NEVER their warehouse, because `_render_tree` gated leaf emission on `len(grp) > 1 or filters.get("show_single_wh_breakdown")` — and that filter never existed. Fix: always emit the warehouse leaf (item rows `is_group=1`; leaf loop guarded by `if grp:`), faithful to the CC → Item → Warehouse design (user chose "always show WH leaf"). PIVOT mode unchanged; no double-count (value lives on `is_group=0` leaves which `add_total_row` sums). guardian CLEAR (report not locked); demo regression 17/17; code-tester 55/55 edge + 970/970 reconcile (0 items with no leaf); security N/A (display-only). Lightweight track, 1-file logic fix + version. **Shipped to production on the user's "deploy to production".** |
| **2.14.1** | 30 May | **Material Transfer/Issue MR — zero-valuation HARD BLOCK on approval.** Fixed a bug where `approve_transfer()` set `ts_mr_status="Approved"` even when the auto draft Stock Entry failed on a ₹0/missing valuation rate — leaving the MR stuck "Approved" with no Stock Entry and no action buttons (reported via MR `BBPL-TRAN-MEC-26-00008-1`). `ts_mr_transfer.py` now runs `_check_item_valuations` / `_block_if_missing_valuation` to **hard-block** approval with a popup ("Valuation Rate Required") naming every item lacking a valuation rate in its source warehouse, **before any mutation** — user must set the rate (Stock Reconciliation) first (no zero-value escape). Status is now **atomic** — `ts_mr_status="Approved"` is set ONLY when the draft Stock Entry actually exists, else it throws (status stays recoverable) — structurally eliminating the stuck state. NEW read-only `@frappe.whitelist()` `get_transfer_valuation_warnings` (perm-gated, escaped) drives a red form banner in `mr_approval.js` via the existing `_show_ts_banner`. Scope: Material Request **Transfer + Issue** only; PO-side warning deferred (needs PO_FULL unlock). 3 files, +154/-16. **Phase 4 ALL GREEN (demo):** guardian (MR_FULL unlocked→relocked, verify-locks 1/1); security **0C/0H/0M/0L** (IDOR-safe read gate, CSRF-safe per L175, all DB strings escaped both Python + JS, tamper guard untouched); code-tester **98/100** (2 non-passes = test-harness artifacts; old bug proven gone, happy path intact Transfer + Issue); demo regression **17/17 BEFORE+AFTER**. **SHIPPED TO PRODUCTION 30 May 2026** (commit `3d8459a`; prod CLEAN fast-forward `b379454→3d8459a`). Pre-deploy backup `20260530_190329-erpbbpl_com-database.sql.gz`; `__pycache__` clear (L180) + `bench build` + `clear-website-cache` (L174) + web/workers restart (sessions preserved, not `bench clear-cache`). **Post-deploy ALL GREEN:** version 2.14.1 ✓, md5 `c3652e78d13d320e7a5a6929203055c6` ✓, 3 functions importable ✓, **prod regression 17/17** ✓; live proof — endpoint on the real stuck doc flags **4** zero-val items (`10-45-0383`, `10-45-1181` "C.I. Pin Bush Coupling 10″", `10-45-0592`, `10-45-0813`, all ₹0 in Stores - BBPL). NEW **Lesson 288** (atomic status with side-effect). **STILL OPEN:** stuck doc `BBPL-TRAN-MEC-26-00008-1` recovery — preventive fix does NOT auto-heal it; needs valuation rates set + one-off status flip to re-approve. |
| **2.14.0** | 30 May | **TS Material Issue Ledger — branded PDF + Excel export, Material Transfer coverage, Use Location + Item Remark captured on Stock Entry, CEO/MD access.** The report previously covered only Material Issue Stock Entries; it now covers **Material Issue + Material Transfer**, adds an **Item Remark** column, and sources both **Use Location** and **Item Remark** from `Stock Entry Detail` (preferred) with a fallback to the originating MR line. New standalone **`use_location`** filter and a summary-card currency fix. **Two new export endpoints** on the report toolbar: **Export Branded PDF** (`export_material_issue_ledger_pdf` in `ts_pdf_export.py` — NEW branded A4-landscape Jinja template `pdf_template.html`, 17 columns, totals row, status pills; the existing FIFO export is byte-untouched, imports aliased) and **Export Excel** (`export_material_issue_ledger_excel` in the NEW `ts_excel_export.py` via `make_xlsx`). Both endpoints are `@frappe.whitelist(methods=["POST"])` (CSRF-safe, Lesson 175) and gated with explicit `frappe.has_permission("Stock Entry", "read")` (Lesson 224) — a raw HTTP GET returns 403. **Schema:** `setup.py` `create_custom_fields()` gains a `Stock Entry Detail` block adding `ts_delivery_location` ("Define Use Location") + `ts_item_remark` ("Item Remark"), both `fetch_from material_request_item.*` and shown only for Material Issue / Material Transfer via `parent.purpose` depends_on. Report roles += **CEO + MD**. **Phase 4 gates ALL GREEN (demo):** guardian CLEAR (`setup.py` unlocked with user authorization for MR_FIELDS / MR_FULL / PO_FULL, edited additively — MR/PO blocks byte-untouched, 30/0 numstat — then RE-LOCKED, verify-locks.py 14/14); security **COMMIT 0 Critical / 0 High / 0 Medium / 0 Low** (scanned both endpoints + schema/report change); code-tester **60/60 (export delta) + 26/26 (schema delta)** with HTTP GET→403 confirmed; live-data-tester **10/10 GO** (991 Stock Entries load, report 0.2s, zero data mutation); predictor MEDIUM (proceed); demo regression **17/17 PASS BEFORE and AFTER** (multiple runs). 8 files, +214/-39 LOC tracked + 2 new files (`ts_excel_export.py`, `pdf_template.html`). All 8 files md5-identical local↔demo. **SHIPPED TO PRODUCTION 30 May 2026** (commit `b379454`; prod `git reset --hard upstream/develop` `094e190→b379454` — prod tree was dirty from the directly-applied v2.13.1 hotfix, all edits superseded, Lesson 230). Pre-deploy backup `20260530_134400-erpbbpl_com-database.sql.gz` (10 MiB + files); `bench migrate` created the Stock Entry Detail fields cleanly; `__pycache__` clear (L180); web+workers restart; **report CEO+MD roles applied via explicit `doc.save()` — `bench migrate`'s reload_doc did NOT sync the standard Report's `roles` child table (Lesson 281 extends from Workspace to the Report doctype)**; sessions-preserving meta flush (L234, never `bench clear-cache` on prod). **Post-deploy ALL GREEN:** version 2.14.0 ✓, SE Detail fields + depends_on + fetch_from ✓, report roles incl. CEO+MD ✓, exports POST-only ✓, FIFO export `get_data` unshadowed ✓, CEO `erp.superadmin@betulbiofuel.com` SE_read=True ✓, **report 369 rows for company `Betul Biofuel Pvt. Ltd.`** (prod has 2 companies; the empty `CF Betul Biofuel Private Limited` is not the operational one) ✓, **prod regression 17/17** ✓, post-deploy audit GREENLIGHT 0 issues (config unchanged, clean tree, cross-server parity, hash chain 32/0 breaks). |
| **2.13.1** | 28 May | **Client JS gate mirror — cascade-delete page reveals token-search row to Stock User + Stock Manager (v2.13.0 oversight fix).** v2.13.0 expanded server-side `_INITIATE_ROLES` to include `Stock User` + `Stock Manager` (commit `094e190`), but the client JS gate at `cascade_delete.js:200` was still hardcoded to `user_roles.has("Super Admin")` only — Stock User / Stock Manager could open the page (page-roles JSON was updated) but the token-search row stayed `display:none`, so they had no way to actually use the feature. Surfaced when `prateek.srivastav@betulbiofuel.com` tried it on prod. **Fix (3 LOC):** `can_initiate` now ORs `Super Admin` + `Stock Manager` + `Stock User`, mirroring the server's `_INITIATE_ROLES`. Server-side `_gate_role` at `cascade_delete_api.py:42-46` remains the authoritative security boundary — this JS change is UX visibility only. `CD_PAGE_VERSION` bumped `v2.11.2-2026-05-25-2 → v2.13.1-2026-05-28` per Lesson 174 cache-flush verification. Lightweight track: 2 files, ~5 LOC. **Deployed to demo AND prod earlier today** (sequential `bench build` + `clear-website-cache` per Lesson 174); pre-fix prod backup `20260528_132441-erpbbpl_com-database.sql.gz` (9.4 MiB). Security / code-tester / predictor SKIPPED per lightweight scope-card (UI-visibility-only, no server logic change — v2.13.0's 120/120 already verified Stock User can initiate via API). |
| **2.13.0** | 28 May | **Cascade Delete role expansion — Stock User + Stock Manager can initiate; engine runs as resolved Super-Admin executor.** User request: "store users need permission to send cascade requests for approval". Until now, only `Super Admin` could call `initiate_cascade` — the store team had to ping a Super-Admin to file every cascade request. **Mechanism:** `_INITIATE_ROLES` widened to include `Stock User` + `Stock Manager` (same force-flag entitlements as Super Admin per user choice — Option C, no Super-Admin pre-review layer added); NEW `_EXECUTOR_REQUIRED_ROLES = {"Super Admin"}` declares which roles the engine MUST run as during destructive `ceo_approve_cascade` execution; NEW helper `_resolve_executor_user(initiator)` returns the initiator if they hold `Super Admin`, else falls back to `ts_settings.ts_cascade_executor_user` (new permlevel=1 Link → User in TS Settings), else falls back to the first enabled user with `Super Admin` role, else throws `PermissionError("no executor configured")` — fail-closed by design. `ceo_approve_cascade` calls the resolver, switches to the resolved user via `frappe.set_user()` for the duration of the destructive section ONLY, then restores; the resolved user is persisted to the log as `executed_as` (new permlevel=1 Link → User on `TS Cascade Delete Log`) for forensics. **Hash chain note:** `executed_as` is DELIBERATELY EXCLUDED from `_HASH_FIELDS` — same precedent as `cut_point`; this keeps the existing 57 demo logs verifiable on next nightly integrity sweep. **2-person rule preserved** — initiator ≠ approver is the only constraint (user choice). Page roles on `cascade_delete.json` widened to Stock User + Stock Manager so they can open the page. **Operational note for prod:** prod currently has NO user assigned the `Super Admin` role; Stock User/Manager requests will fail-closed with the clear `no executor configured` error until admin either (a) assigns `Super Admin` to a user OR (b) sets `ts_cascade_executor_user` in TS Settings. Safe — nothing destructive can happen without a resolvable executor. **Phase 4 gates ALL GREEN:** guardian no locked files; security delta scan APPROVE COMMIT 0C/0H/0M/0L/2I (parameterized SQL + Lesson 162 permlevel + Lesson 224 dual-gate all verified); code-tester **120/120 PASS (100%)** across 8 categories (role matrix, executor resolution, 2-person rule, hash-chain integrity, audit forensics, rate-limit, Lesson-224 delete-perm, disabled-executor fallback); functional smoke 6/6; predictor CRITICAL flags all addressed (forensics captured in initiate before any set_user, hash exclusion documented, 2-person rule choice documented, live-data-tester subsumed by code-tester); demo regression **17/17 PASS BEFORE and AFTER**. 6 files, +140/-23 LOC. **Develop-only ship at commit time** — user will live-test on demo then explicitly say "deploy to production". |
| **2.12.0** | 26 May | **NEW Frappe Script Report: TS Item - Warehouse Wise (pivot ↔ tree auto-switch).** Inventory pivot/tree report listed under the existing "Stock Reports" workspace, surfacing items × warehouse × cost center with stock qty + value as of any date. **Dual-layout engine** auto-switches between PIVOT (≤ threshold active warehouses → warehouses as columns, one row per item × cost-center) and TREE (> threshold → hierarchical Cost Center → Item → Warehouse). Threshold is admin-tunable via the new TS Settings field **`ts_pivot_warehouse_threshold`** (Int, permlevel=1, default 10) — designed against today's prod reality (5 active warehouses, pivot is right-sized) while being future-proof if BBPL adds warehouses. **8 filters:** Company, As-on Date, Warehouse (MultiSelectList), Cost Center (MultiSelectList), Item Group (MultiSelectList), Item (MultiSelectList), Force Layout (Auto/Pivot/Tree), Show Zero-Stock toggle, Show Offsetting CC Movements toggle (hides net-zero per-CC pairs by default — same-item movements between cost centers within a warehouse that net to zero are collapsed). **6 roles:** System Manager, Stock Manager, Stock User, Accounts Manager, CEO, MD. Engine reuses the production-proven SLE↔GLE join pattern from `ts_stock_ledger_fifo.py:71-89`. Total row enabled (`add_total_row=1`); only `stock_value` is summed — all qty columns carry `disable_total: 1` because items mix UOMs (Kg + Quintal + MT + Nos + Mtr → cross-UOM sum would be physically meaningless). **Bug fixes during user testing on demo (all rolled into this commit):** (a) folder rename `ts_item_warehouse_wise/` → `ts_item___warehouse_wise/` — Frappe's `scrub()` of "TS Item - Warehouse Wise" produces 3 underscores; report wouldn't load until the path matched the scrub output; (b) MultiSelectList filters needed `options: "<DocType>"` per filter (Warehouse / Cost Center / Item Group / Item) — without the option string Frappe couldn't resolve the autocomplete query; (c) Total row enabled via `add_total_row: 1` + the previously-hardcoded "Grand Total" footer row removed + `disable_total: 1` added to percentage columns and Valuation Rate (averages, not sums); (d) summary-card fix — removed `currency=filters["company"]` from the value card which was displaying the company NAME as the value. **Dry-run validation on demo:** 6/7 checks PASS — 360ms runtime on full warehouse set, 938/938 (item, warehouse) pairs reconcile to ERPNext's native Stock Balance with 0 drift, 1 benign finding (net-zero CC pairs hidden by default — refinement, not a defect). **Phase 4 gates ALL GREEN:** guardian no locked files; security delta scan APPROVE COMMIT 0C/0H/0M/0L/2I (the 2 INFO findings are safe constant f-string interpolation, manually verified); demo regression **17/17 PASS BEFORE and AFTER**. Full track: 4 new files + 3 modified, ~577 LOC (within 2× scope budget). **Develop-only ship at commit time** — prerequisite for the production deploy that follows. |
| **2.11.2** | 25 May | **Partial cascade re-enabled — derive-from-deepest-surviving + fail-closed pre-cascade validator (audit B1 + orphan scanner + revert path).** v2.11.1 had to disable partial cascade because the planner's static `cut_point → reset_status` map was wrong — live demo had 6 Tokens at legacy/post-exit statuses the planner had called "dead" (`Quality Done`, `Graded`, `Unloading`, `Exited`), and the static map would have written a wrong status on partial cancellation. v2.11.2 fixes B1 properly: **rewrote `_reset_token_status()`** to derive the post-cascade status from the deepest-surviving document (PI → PR → DS → QI → WB → GE) at execute time, with a TOCTOU re-check after deletion and a survivor cross-check — FAIL-CLOSED on any unmapped pair (no silent wrong-status writes). **New `_validate_partial_cut_feasibility(token_name, cut_point)`** runs BEFORE backup/snapshot/approval-flow: rejects ABORT cells in `_CD_PARTIAL_MATRIX` (the 13-cell allowlist defining which `(current_token_status, cut_point)` pairs are safe), rejects legacy/post-exit statuses, rejects pre-chain statuses, rejects stock-out states (GL/SLE would orphan). **User-approved decisions:** ABORT-on-legacy (statuses outside the matrix must use Full Chain); engine wins on GE cut (Token will be DELETED when cut-point = TS Gate Entry — JS preview now matches engine reality, fixing the v2.11.1 mismatch); orphan-scanner + revert path in scope. **`scan_orphans()`** is now cut-point-aware: kept documents above the cut-point are no longer false-flagged as orphans (reads `log_doc.cut_point` via `cascade_delete_integrity.run_orphan_scan_for_log`). **`restore_from_backup()`** in `cascade_delete_backup.py` now restores `Token.status` (+ `purchase_receipt`, `grn_time`, `quality_time`, `wb_*_time`, `custom_rst_number`) from the pre-cascade backup when the Token survived a partial cascade (revert path). **Removed the v2.11.1 server-throw** on `cut_point != "Full Chain"` in `cascade_delete_api.initiate_cascade`. **Page (`cascade_delete.js`):** removed the yellow "disabled" banner; un-disabled cut-point radios; new client-side `_CD_PARTIAL_MATRIX_JS` for instant preview; new red `cd-warn-note` shown when an ABORT cell is picked; GE-cut preview now correctly says "Token will be DELETED"; 3-line CSS additions (`<code>` styling + `overflow-wrap: anywhere`); `CD_PAGE_VERSION` → `v2.11.2-2026-05-25-2`. **Phase 4 audits ALL GREEN:** predictor RISK CRITICAL (handled with full pipeline); guardian PASS (no locked files touched); security APPROVE COMMIT — **0 CRITICAL / 0 HIGH / 0 MEDIUM / 0 LOW / 0 INFO**; code-tester **237/237 PASS (100%)**; ui-designer APPROVE after 5-LOC CSS fix; demo regression **17/17 PASS BEFORE and AFTER**. **3 new lessons:** **278** — state-machine inverse mappings must DERIVE from observed state, never trust a static planner-built map (live data falsified the v2.11.1 map); **279** — pre-cascade feasibility validators must FAIL-CLOSED on any unmapped (current_status, cut_point) pair before backup/approval/email; **280** — when re-enabling a feature that was disabled because a hook was wrong, mirror the production-proven hook pattern (derive-from-survivor) into the new path. 6 files, +453/-93 LOC (within 2× scope budget of 260 target). Develop-only ship; v2.11.2 is the **REQUIRED prerequisite** for the production deploy of the full v2.11.0→v2.11.2 cascade stack. |
| **2.11.1.1** | 23 May | **DS→PI propagation timing-bug fix — close the cross-document submit window.** The DS controller's `_propagate_to_pi` runs in DS `on_submit`; when the DS is submitted BEFORE the related PI is created, the propagation iterates an empty PI set and the `ts_ds_actual_deduction_pct/kg/reason` + `ts_ds_reference` fields never reach that PI. Diagnostic case: DS `BBF-DED-2026-00310` submitted at 12:06:11, PI `PINV-26-00230` created at 12:06:43 (32s later) — fields stayed empty even though PI `PINV-26-00229` (created earlier) was populated correctly. **Fix:** extended `_snapshot_receipt_context_from_pr()` in `ts_pi_override.py` (gated by the existing one-shot `if ts_token: return`) to mirror the DS-side propagation from the PI side — after linking the DS, pull `actual_deduction_pct/kg/reason` via `frappe.db.get_value` and set the four display fields when `docstatus==1`. Best-effort: wrapped in `try/except` (Lesson 238) with `frappe.log_error(title=, message=)` kwargs (Lesson 237) so a DS-fetch failure can never break PI validation. Retroactive fix already applied on demo via `ds.run_method("_propagate_to_pi")` for BBF-DED-2026-00310 (both PINV-26-00229 + PINV-26-00230 now carry the correct values + `[DS_APPLIED]` audit comments). **Phase 4 audits:** predictor RISK LOW (read-only display fields, no GRN/approval/financial logic touched, rollback EASY); code-tester PASS 53/54 functional + 17/17 regression = 70/71 (98.6%; the 1 failure is fixture exhaustion, not fix-related); security APPROVE COMMIT 0C/0H/0M/0L/2I (parameterized SQL + Lesson 237/238 correctness confirmed); guardian no locked files affected. Demo regression 17/17 PASS. Lightweight track: 1 file, +27 LOC. **Develop-only ship** — production stays at 2.10.2.5. |
| **2.11.1** | 22 May | **Cascade Delete remediation — 10 of 11 audit findings fixed (split ship; B1 → v2.11.2).** Outcome of the 22 May multi-agent audit (security / predictor / code-tester / integrity). **B5** — real webhook SSRF guard: new shared `validate_webhook_url_safe()` resolves the host and rejects on the resolved IP object (private/loopback/link-local/reserved/multicast), catching decimal/hex/octal/IPv6-loopback forms the old substring blocklist missed; re-validated before every dispatch attempt + `allow_redirects=False`; `ts_settings.py` uses the same guard. **B6** — `backup_filename`/`backup_sha256`/`prev_row_hash`/`this_row_hash`/`forensic_block_json`/`target_chain_json` bumped permlevel 0→1 (+ Super Admin permlevel-1 read row); new `_safe_backup_name()` path-traversal guard on every `os.path.join`. **B4** — financial blast-radius gate: `build_chain_snapshot` now detects downstream Payment Entries / Journal Entries / Landed Cost Vouchers / billed PRs; `execute_cascade` BLOCKS before any deletion unless the new `force_payment_links` override (type-to-confirm `FORCE-DELETE-WITH-PAYMENTS`) is set; the preview + CEO review panel surface the blast radius. **B3** — surgical-SQL trigger now matches the GE guard's EXACT message (was a loose `"Quality"` substring that could mis-classify any error into a force-delete); widened cleanup removes child-table/DocShare/ToDo rows. **B2** — corrected the false docstrings: the GE `on_cancel` guard does NOT honour `cascade_delete_mode`, so surgical-SQL is the formally-accepted PRIMARY GE-deletion path. **M1** — honest atomicity docs + `partial_failure` reporting in `_abort`. **M3** — no Administrator fallback when the initiator is disabled (abort + mark Failed). **M4** — unknown `cut_point` fails closed (throw) instead of defaulting to full cascade. **M5** — `setup_super_admin_role` refuses + logs CRITICAL rather than insert the FIRST Custom DocPerm row on a doctype (Lesson 169 blackout guard). **M2** — admin-configurable revert window (`ts_cascade_revert_window_minutes`, 5–60 min) + UI warning that revert restores documents only, not GL/SLE. **Partial cascade temporarily DISABLED** (server hard-rejects non-Full-Chain; cut-point selector greyed out) — the `_reset_token_status` rewrite (B1) + re-enable ships in v2.11.2. 11 files. Demo + develop only; the v2.11.0 cascade stack stays paused for production. |
| **2.11.0.4** | 22 May | **Cascade rate-limit fix — configurable TS Settings limit now the sole authority.** Removed the hardcoded `@rate_limit(key="cascade_initiate", limit=5, seconds=172800)` decorator from `initiate_cascade()` in `cascade_delete_api.py`. The fixed HTTP-layer decorator IGNORED the admin-configurable TS Settings limit (`ts_cascade_rate_limit_count` / `_seconds`), silently overriding it and blocking the team's cascade-delete testing with "You hit the rate limit because of too many requests". The configurable in-body check (per-user, window-based, counts persisted `TS Cascade Delete Log` rows so it survives restarts — see `rl_count` / `rl_seconds`) is now the single authoritative rate limit; an explanatory comment replaces the decorator to prevent re-adding a fixed one. `@frappe.whitelist(methods=["POST"])` retained. **Security APPROVE COMMIT** — 0 CRITICAL / 0 HIGH / 0 MEDIUM / 0 LOW / 1 INFO (the INFO, a stale comment, was fixed in this same delta); removing the decorator does NOT under-protect the endpoint — the configurable in-body rate limit, kill switch, Super Admin role gate, type-to-confirm, in-flight duplicate guard, dual `has_permission`, POST CSRF, and 2-person CEO approval all remain. Demo regression 17/17 PASS; deployed to demo (`__pycache__` cleared, supervisor restarted), team confirmed unblocked. Lightweight track: 1 code file, 1 decorator line removed / ~8 comment lines changed. |
| **2.11.0.3** | 21 May | **BBPL Purchase Order print format — 3 changes (develop-side fold-in of the v2.10.2.5 prod hotfix).** Shipping Address row added below Project / Material Request; Payment Term column added before Description in the Payment Terms table; header "Amount Payable" → "Payment Amount". These shipped to **production as the standalone hotfix `3c836e2` (v2.10.2.5)** branched off `2a16693` so prod received ONLY the print-format change, not the parked v2.11.0 cascade stack — this row folds the identical `bbpl_purchase_order.json` change onto the develop line so the eventual cascade prod deploy keeps it. All 3 new interpolations carry the `\| e` escape filter (Lesson 273). Security 0/0/0/0/2 INFO APPROVE. Regression 17/17 demo + prod. Render-tested on real demo + prod POs. **Deploy note:** because the v2.10.2.5 hotfix is a sibling of the cascade commits (both off `2a16693`), prod is now at `3c836e2` which is NOT an ancestor of develop — the eventual v2.11.0 cascade prod deploy must use `git reset --hard upstream/develop` (prod tree clean), not a fast-forward `git pull`. |
| **2.11.0.2** | 20 May | **4 soak-window fixes (point release within the v2.11.0 line — base `37f4f25` + `28dc451` not yet on prod; all three commits deploy together)** — pre-deploy soak batch from the cascade-delete tool's testing window. **Fix #16 — in-flight duplicate-cascade guard:** `initiate_cascade()` now blocks a second cascade on a token whose `TS Cascade Delete Log` is still `Pending CEO Approval` or `Approved`; the operator must let it resolve (execute / fail / reject / cancel) before starting another. **Fix #17 (new Lesson 276) — leaked side-effect message suppression:** `frappe.clear_messages()` is called before the success-returns of `initiate_cascade()` and both branches of `ceo_approve_cascade()`, so best-effort side-effect noise (a missing-Email-Account msgprint, the Lesson 275 GE-cancel guard throw the engine already recovered from via surgical SQL) never surfaces as a scary popup — the cascade outcome is returned structured (Lesson 238). **Fix #14 — stale-preview clear:** after a successful initiate the page clears `#cd-preview-area` and `#cd-token-input` so the operator can't re-initiate the same token; the new entry appears under Pending CEO Approval. **Fix #15 — auto-expire Revert button:** `_render_recent_list()` schedules a single deadline-targeted `setTimeout` at the earliest still-future 5-min revert-window deadline, re-rendering the Recent list so the Revert button flips to "expired" without a page refresh (timer cleared + rescheduled each render; delay clamped to the 32-bit `setTimeout` max). `CD_PAGE_VERSION` pill bumped to `v2.11.0.2-2026-05-20`. **Phase 4 audits:** security APPROVE COMMIT — 0 CRITICAL / 0 HIGH / 0 MEDIUM / 0 LOW / 2 INFO (both non-blocking: `clear_messages` also clears legitimate warnings note; `setTimeout` 32-bit clamp note). E2E verified on demo: Fix #16 guard (1st initiate OK, 2nd BLOCKED); Fix #17 (`message_log` 1→0, cascade log 09267 confirmed Executed); WB tare tie-break re-verified 8/8 deterministic. Demo regression 17/17 PASS after each fix. Lightweight track: 2 code files +54/-1 LOC. |
| **2.11.0.1** | 20 May | **Partial Cascade ("cut-point deletion") + 13 soak-window fixes** — point release within the v2.11.0 line (base `37f4f25` not yet on prod; both commits deploy together). **Partial cascade:** the cascade-delete tool can now stop at a user-chosen cut-point instead of always deleting the full PI→PR→DS→QI→WB→GE→Token chain. New `_DELETION_STEPS` ordered list + `CUT_POINT_DOCTYPES` set + `_cut_index()` in `cascade_delete_engine.py` gate `execute_cascade()` so only steps at-or-above the cut-point run; the surviving cut-point doc is preserved and `_reset_token_status()` re-snapshots `Token.status` to the correct surviving phase. Includes the **WB tare tie-break fix** — `ORDER BY tare_weight DESC LIMIT 1` makes weighbridge-log selection deterministic (was the lone code-tester failure; now 8/8 deterministic "Tare Weighed" every run → effective 133/133). API layer adds a `cut_point` param validated against `_CUT_POINT_VALUES` whitelist; new `cut_point` Select field on `TS Cascade Delete Log` (permlevel=1, read_only_on_submit, set_only_once — excluded from `_HASH_FIELDS` per guardian). **13 soak fixes:** (engine/api) `_render_cascade_email` rendering fix, `frappe.set_user` wrapped in try/finally, `_REVERT_ROLES` correction; (page) DOM trimmed with body/CSS moved into JS, modal appended to `#cd-root`, timestamp formatting, status legend, Revert column, expandable review row, radio cut-point selector with row tinting + plain-English summary; (Lesson 169) `TS Cascade Delete Log` removed from `_SUPER_ADMIN_PERMS` in `setup_super_admin_role.py` so Standard DocPerm is preserved. **Phase 4 audits:** guardian APPROVE (no locked files in delta; `cut_point` excluded from hash fields); security APPROVE COMMIT 0 CRITICAL / 0 HIGH / 0 MEDIUM / 1 LOW (non-blocking engine cut_point dual-interpretation, follow-up hardening) / 3 INFO; code-tester 132/133 → 133/133 effective after WB tie-break fix re-verified 8/8 deterministic. Demo regression 17/17 PASS. 6 files +871/-240 LOC. Still under the 50h soak + team walkthrough window; prod deploy pending user's explicit instruction (deploys together with `37f4f25`). |
| **2.11.0** | 20 May | **Token Cascade Delete System** — new custom page `/app/cascade-delete` with hardened cascade-delete tool for Super Admin role. **8 hardening features** (A: hash-chain audit / B: pre-delete JSON snapshot / C: 2-person rule with CEO approval / D: external webhook with HMAC signature / E: DB row-count assertion / F: crypto-signed backup file SHA-256 / G: orphan-trail integrity scanner / M: 5-min revert grace window). **2-person rule** enforced server-side — Super Admin (`erp.superadmin@betulbiofuel.com` single user) initiates, CEO approves (must differ from initiator). **Type-to-confirm gates** at 3 stages: token name + `FORCE-DELETE-PR` + `FORCE-DELETE-MI`. **Rate limit** 5 deletions / 48h rolling window. **Cascade order** PI → PR → DS → QI → WB Log → GE → Token (atomic per step). **Lesson 275 mitigation** embedded in engine — `frappe.flags.cascade_delete_mode = True` try/finally + surgical-SQL fallback (Lesson 262 Option B) when GE cancel guard trips. **Kill switch** `ts_cascade_delete_enabled` defaults OFF on every site (Lesson 227); System Manager must flip ON post-install. **Webhook URL** SSRF-protected (HTTPS-only, no private IPs); HMAC-SHA256 signature. **Full forensic log** per deletion (IP, OS, device, browser, GeoIP, session, timezone, screen, language, UA). **Bold-red email** to MD + CEO on every state transition. **23 files** (~3460 LOC): new submittable DocType `TS Cascade Delete Log` (52 fields incl. hash-chain `prev_row_hash`/`this_row_hash`, forensic block, backup metadata, webhook tracking, revert window, integrity scan) with append-only controller; new orchestration engine `cascade_delete_engine.py` + 4 utility modules (backup / forensics / webhook / integrity); 7-endpoint API `cascade_delete_api.py` (preview / initiate / approve / cancel / revert / scan / verify-chain); 3-file custom page with 3-stage modal flow; bold-red email template; 2 new after_migrate seeders; 2 new scheduler crons (1-min revert expiry + 3am nightly integrity audit). Phase 4 audits: guardian APPROVE (after GATE_ENTRY_DRAFT_OPS + SERVER_CONFIG unlocks); predictor LOW (kill switch OFF default + 2-person + backup mandatory); code-tester 600 scenarios; live-data-tester real demo; ui-designer custom page pass; security 0/0/0/0/x. 50h soak window + team walkthrough required pre-prod. Production deploy: kill switch stays OFF; System Manager flips ON when ready. |
| **2.10.2.4** | 17 May | **BUG FIX — BBPL Purchase Invoice print format Cost Center showed wrong value** (regression in v2.10.2.1 ship 16 May 2026). User reported PI print displayed `'Main - BBPL'` while the form correctly showed e.g. `'RM MAIZE/RICE/DORB - BBPL'`. **Root cause:** v2.10.2.1 PI format Jinja rendered Cost Center as `(ns.first_cc or doc.cost_center or '-') \| e` where `ns.first_cc` = `items[0].cost_center`. ERPNext's Item Master Default Cost Center logic auto-fills every PI row's `items[].cost_center` to the Item's default (e.g. `'Main - BBPL'`) when the user doesn't explicitly pick row-level CC — overriding the header. Result: items[0]='Main - BBPL' wins, masking the authoritative `doc.cost_center` value. Verified on prod: all 8 latest PIs (PINV-26-00344..00351) had header='RM MAIZE/RICE/DORB - BBPL' AND row='Main - BBPL' simultaneously. **Why PI differs from MR** (the v2.10.2.3 fix prefers `ns.first_cc` for MR): MR has `_mr_before_save` server-side mirror that overwrites every row's `cost_center` with `doc.cost_center` on save — so MR items[0] is reliably aligned with header. PI has no such mirror; ERPNext's Item Default fallback wins. The two formats need OPPOSITE priorities. **Fix:** swap precedence to `(doc.cost_center or ns.first_cc or '-') \| e` — prefer the authoritative form-displayed `doc.cost_center`, fall back to `ns.first_cc` only when header is null (edge case). 1-char order swap; keeps `\| e` filter (Lesson 273) and `namespace()` block intact. Demo-verified smoke + code-tester 30 scenarios + security 0/0/0/0/0 APPROVE COMMIT. Lightweight track: 3 files (~3 LOC change). Predictor LOW. Guardian APPROVE (no locked files). Regression 17/17 BEFORE + AFTER. |
| **2.10.2.3** | 17 May | **BUG FIX — BBPL Material Request print format Cost Center cell always rendered empty** (silent regression since v2.10.0.7 ship 3 May 2026). User reported via `PR-HR-26-00022-1` and "others". **Root cause:** Jinja for-loop scoping bug at line 10 of `bbpl_material_request.json`: `{% set first_cc = '' %}{% for it in doc.items %}{% if it.cost_center and not first_cc %}{% set first_cc = it.cost_center %}{% endif %}{% endfor %}` — Jinja2's `{% set %}` inside a `{% for %}` creates a loop-local variable; the outer `first_cc` is never updated. Cost Center cell always rendered empty regardless of `doc.cost_center` or `items[].cost_center` values. Same broken pattern was the v2.10.0.7 author's accidental deviation from the namespace() pattern correctly used in BBPL PR/PO/PI sibling formats. **Fix:** rewrite as `{% set ns = namespace(first_cc='') %}{% for it in doc.items %}{% if it.cost_center and not ns.first_cc %}{% set ns.first_cc = it.cost_center %}{% endif %}{% endfor %}` + cell renders `{{ (ns.first_cc or doc.cost_center or '') \| e }}` — adds defensive header-level `doc.cost_center` fallback for MRs without row-level CC, plus `\| e` escape filter per Lesson 273 (scoped defense-in-depth for the one field touched; broader autoescape patch on BBPL MR/PR/PO formats remains a separate backlog ship). Demo verified on PR-HR-26-00022-1 (CC='HR, safety & and Furniture Material - BBPL'): pre-fix render → empty cell; post-fix render → CC value present. Pure 1-line Jinja edit. Lightweight track per scope-card: 3 files (~5 LOC). Predictor LOW. Guardian APPROVE (no locked files — `bbpl_material_request.json` is NOT in MR_PRINT_BUTTON / MR_FULL `protected_files`; only `TS Material Request` + `TS Material Request (Clean)` print formats are locked). |
| **2.10.2.2** | 16 May | **BBPL Purchase Invoice — Item Remark column** — adds one column to the items table in the v2.10.2.1 print format, rendering `item.ts_item_remark` per row (Data Custom Field on Purchase Invoice Item, auto-fetched from PO Item via `setup.py:762 fetch_from=po_detail.ts_item_remark`). Column inserted between Item and HSN, width 85px (matches v2.10.0.6 BBPL PO Item Remark pattern). Other column widths rebalanced (S.No 32→30, HSN 60→55, Quantity 60→55, Unit 45→40, Rate 75→70, GST 88→85) so the 8-column table still fits A4 — total fixed widths 515px + Item flex ≈ 220px. tfoot colspans bumped 7→8 (Net Total + each tax row + Grand Total). **XSS-safe** per Lesson 273 (just landed in v2.10.2.1) — uses `{{ (item.ts_item_remark or '') \| e }}` with parens for Jinja filter precedence. Pure additive Jinja edit. Lightweight track (~10 LOC across 3 files): print format JSON (+5 LOC: 1 `<th>`, 1 `<td>`, 4 colspan bumps), `__init__.py` version 2.10.2.1 → 2.10.2.2, README ship row. NO Python / NO JS / NO Custom Field / NO hook / NO seeder / NO workspace touch. Skipped planner / ui-designer / code-tester / live-data-tester (validated 3 hrs ago on parent template surface); kept guardian + security + 17-test regression. |
| **2.10.2.1** | 16 May | **BBPL Purchase Invoice print format + 🖨 Print PDF toolbar button** — new branded Jinja print format `BBPL Purchase Invoice` mirroring the v2.10.0.4 BBPL Purchase Receipt skeleton (`<img src="/files/BBPL-LH.png">` letterhead embed per Lesson 232 — avoids wkhtmltopdf flex collapse). Title bar reads **TAX INVOICE** (or **DEBIT NOTE** when `doc.is_return=1`). Surfaces the user's 4 requested fields in a dedicated **Receipt Context** block: **Token No** (`doc.ts_token`), **RST No** (`doc.ts_rst_number`), **PO No** (`doc.ts_po_reference` — already auto-comma-separated for multi-PO via v2.8.6 `populate_ts_po_reference` validate hook), **PR No** (Jinja namespace loop over distinct `doc.items[].purchase_receipt` — comma-joined; falls back to `doc.ts_purchase_receipt` header snapshot if items-row PR is empty). Auto-shows count `PR No (3)` when multi-PR. Plus conditional QI / DS / QC Status rows below when populated (from v2.9.16.4 snapshots). **Conditional QC Override callout** — yellow-bordered block rendered ONLY when `doc.ts_qc_override=1`, surfaces reason / approved-by / approved-at (PI's distinctive audit signal vs PR). **XSS hardening (CRITICAL — Lesson 273 candidate):** runtime probe on demo confirmed Frappe v15's `frappe.utils.jinja.get_jenv().autoescape == False` even for `print_format_type=Jinja`. Initial template was vulnerable (`<script>` payload in `doc.bill_no` rendered raw). Patched with explicit `\| e` escape filter on all 30 user-data interpolations using `(expr or '-') \| e` form (parens essential — Jinja filter precedence binds tighter than `or`). Two `\| safe` filters retained intentionally on `doc.terms` + `item.description` (rich-text editor content). Re-verified post-fix: same `<script>` payload now renders as `&lt;script&gt;`. **Note:** the same latent XSS pattern likely exists in `bbpl_purchase_receipt.json` / `bbpl_purchase_order.json` / `bbpl_material_request.json` — flagged for a separate maintenance ship; not in this v2.10.2.1 scope. **Items table** with HSN column added (PI is GST-facing, B2B convention) — S.No / Item / HSN (`item.gst_hsn_code`) / Qty / Unit / Rate / GST / Amount. Per-item GST cell parses Item Tax Template name with `doc.taxes[0].rate` fallback; IGST vs GST label flip based on account_head substring. `white-space:nowrap` on all ₹ cells (Lesson 232). **Payment Terms** block renders flat (no inclusive/exclusive GST toggle — that's PO-only v2.10.0.8; PI doesn't have the field yet) with Description-priority fallback to linked Payment Term template (v2.10.0.6 pattern). **Static yellow ⚠ note** at bottom: "Please mention this invoice number in your payment advice.". DRAFT / CANCELLED watermarks per docstatus. Signatures: Prepared By (Accounts) / Authorised Signatory. BBPL footer. **JS toolbar button** added in `pr_pi_columns.js` — new `_add_bbpl_pi_print_button(frm)` helper + wired into existing PI `refresh` hook; appears on **submitted** PIs only (`docstatus==1`, hides for draft/cancelled — mirrors PR pattern). Click downloads PDF via `frappe.utils.print_format.download_pdf`. **Zero schema/lifecycle touch:** no Custom Fields, no hooks.py, no validate/before_save/on_submit, no Property Setter, no seeder, no DocPerm, no workspace. Pure additive print surface. All 4 user-asked fields already existed on PI via v2.9.16.4. 5 files: 2 NEW (print_format folder + .json), 3 MODIFY (`pr_pi_columns.js` +18 LOC, `__init__.py` version, README ship row). ~250 LOC total (+15 LOC over initial estimate after adding 30 escape filters). **Phase 4 audits:** security 0/0/0/0/1 INFO APPROVE (after XSS fix + re-scan); code-tester 576/576 PASS (after fix; initial 566/576 caught the XSS); live-data-tester 31/31 PASS on real demo data (8 diverse PIs, 5 user roles, 3 actual PDFs 152-154 KB); ui-designer APPROVE (sibling visual consistency, Lesson 232 compliance); predictor LOW; guardian APPROVE (no locked files). Demo regression 17/17 PASS BEFORE + AFTER + AFTER patch. |
| **2.10.2.0** | 16 May | **Accounts Pending Billing Dashboard** — new custom page `/app/accounts-pending-billing` for the Accounts team listing every submitted Purchase Receipt (`docstatus=1`) where `per_billed<100 AND status!='Closed' AND is_return=0`. Single-section table sorted by oldest `posting_date` first (max 500 rows). Each row exposes PR# + Posting Date + Supplier + PO (first PO link + `(+N more)` tooltip when multi-PO) + Grand Total + Billed Amount + `% Bill` badge (red 0%, amber 1–50%, green 51–99%) + Age (days) + `[Open]` link + `[Create PI]` button. Aged rows (>30 days) get a red-left-bordered "⚠ >30 days unbilled" strip. Inline "Has draft PI: <name>" amber pill when a Draft PI already references the PR — prevents accidental duplicate creation. Item-pill expander row shows first 8 PR items with qty + UOM. **3 summary badges** at top: Pending count (blue), Partial count (amber), `>30 Days` count (red). Header has a Refresh button. **`[Create PI]` flow:** confirm dialog → POST to `create_pi_from_pr` → wraps ERPNext's native `make_purchase_invoice` mapper (preserves taxes, project, cost center, payment terms, supplier addresses, MR/PO lineage) → `pi.insert()` Draft (docstatus=0) → JS `frappe.set_route` to the new PI form for Accounts to review + submit. **Auditor role added as read-only** — separate `setup_apb_perms.py` seeder (wired via `after_migrate`) inserts a Custom DocPerm granting `Auditor` `read`/`report`/`export`/`print`/`email`=1 on Purchase Receipt (idempotent, skips if Role missing or row exists; isolated from MR_FULL/PO_FULL-locked `seed_data.py`). JS hides `[Create PI]` button when payload reports `auditor_readonly=true`; backend independently rejects via `frappe.has_permission("Purchase Invoice","create",throw=True)` BEFORE the FOR UPDATE lock (Lesson 224). **Security:** `methods=["POST"]` on the mutation (Lesson 175); regex `^[A-Za-z0-9\-/_]{1,140}$` input validation on `pr_name`; `SELECT … FOR UPDATE` row lock + post-lock state re-check (docstatus / status=Closed / is_return / per_billed≥100); server-side `frappe.utils.escape_html()` on every string returned to the browser (supplier_name, item_code, item_name, draft_pi, po, currency, company); JS additionally re-escapes via `frappe.utils.escape_html` on render; `@rate_limit(key="apb_create_pi", limit=30, seconds=60)` from `frappe.rate_limiter`; `frappe.log_error(title=…, message=…)` kwargs (Lesson 237); `pi.flags.ignore_permissions = False` explicit. **Workspace:** `Pending Billing` shortcut (color=Blue, type=Page) inserted at index 1 of TS Accounts workspace; `Accounts User` + `Auditor` added to workspace roles array (previously only `Accounts Manager / System Manager / IT Head`); **stale `parent_page` "Trustbit Ethanol" → "BBPL Ethanol" fix** (latent bug since the v2.6.0 group rename — TS Accounts workspace had been silently invisible in the sidebar unless reached by direct URL; on prod the DB already had the correct value, so this aligns the on-disk JSON). **Phase 4 audits all green:** security 0/0/0/0/1 INFO APPROVE on the bundle; code-tester **513/513 PASS** across 11 categories (permission tampering 78, status edge 40, XSS+SQLi 40, race 30, mapper edge 54, UI 92, CSRF 20, auditor 30, network 30, workspace 28, misc 71); live-data-tester **47/47 PASS** on real demo data (5 user roles, 4 PR shapes, 3-thread FOR UPDATE race producing distinct PINV-26-00090/91/92, real-data E2E walkthroughs by `kiran.kale@betulbiofuel.com` / Admin with Draft PI cleanup); predictor **MEDIUM PROCEED** (pure additive, no GRN/approval/financial-doc creation logic touched, rollback trivial); guardian **APPROVE** (no locked files; `seed_data.py` deliberately avoided via the dedicated `setup_apb_perms.py` module). Demo regression **17/17 PASS BEFORE + AFTER + AFTER Auditor-fix**. 7 new + 3 modified files / ~670 LOC across `accounts_billing_api.py` (302) + page bundle json+html+js (~325) + `setup_apb_perms.py` (45) + `hooks.py` (+1 after_migrate entry) + `ts_accounts.json` (+1 shortcut +2 roles +parent_page fix) + `README.md` + `__init__.py` version bump (2.10.1.0 → 2.10.2.0). Demo verified: 45 pending PRs visible with correct counts {pending:45, partial:4, aged:2}. Lesson 252 hooks-cache flush applied post-restart. |
| **2.10.1.0** | 16 May | **TS Material Issue Ledger Script Report + 2 Number Cards on Stock Reports workspace** — new advanced report under `TS Gate Entry` module mirroring the existing `ts_stock_ledger_fifo` 4-file pattern (`__init__.py` + `.json` + `.py` + `.js`). Dual-source SQL: Material Request Items (type=Material Issue, docstatus=1) as request-side base + LEFT JOIN aggregated Stock Entry Detail (purpose=Material Issue, docstatus=1) fulfillments + UNION pass for standalone Stock Entry Material Issues with no MR origin. Per row shows **Requested Qty / Issued Qty / Pending Qty** with Status indicator pill (Pending=red / Partial=orange / Fulfilled=green / Standalone=blue). 17 columns including Item Code/Name/Group, **Source Warehouse + Use Location** (`ts_delivery_location` from origin MR row), Cost Center, Rate, Value, cascade Remarks (MR row → SE header). 9 filters: Company (reqd), From/To Date (reqd), Cost Center, Item Group, Item, Source Warehouse, Use Location (substring LIKE), Status (post-compute), Include Cancelled. 6 summary cards (Rows, Distinct Items, Total Requested, Total Issued, Total Pending [orange when >0], Total Value Issued). 5000-row truncation cap. `frappe.has_permission("Stock Entry","read",throw)` gate + `build_match_conditions()` per query. All user filters bound via `%(name)s` params (no SQL injection). Roles: System Manager, Stock Manager, Stores Manager, Stock User, Accounts Manager. **Workspace integration:** 2 NEW Number Card tiles at top of Stock Reports — (1) "Pending Material Issues" (orange #f97316, Document Type NC on Material Request, count of type=Material Issue + status in [Pending/Partially Ordered/Partially Received], demo count=17), (2) "Material Issues This Month" (green #16a34a, Document Type NC on Stock Entry, count of purpose=Material Issue + `posting_date Timespan "this month"`, demo count=16). NC schema gotcha learned + documented: `dynamic_filters_json` with eval-style strings (`frappe.utils.get_first_day(...)`) is REJECTED by Frappe — must use native `Timespan` operator in regular `filters_json`. New shortcut tile (Orange) + new link entry at top of Stock Reports section. Demo verified 31 rows on Betul Biofuel Private Limited Apr–May 2026 (16 Pending + 10 Fulfilled + 5 Standalone); status filters work; NC counts match baseline. Security 0/0/0/0/2 INFO (stylistic only). 6 new + 2 modified files / +515/-3 LOC. 87 sessions preserved through deploy. Prod regression 17/17 PASS. Prod has 0 Material Issue records yet — Number Cards correctly show 0/0 until BBPL Stores Manager creates first Material Issue MR or Stock Entry. |
| **2.10.0.8** | 16 May | **PO Payment Schedule "Including GST" toggle (form + PDF, smart label)** — new header-level `ts_payment_amount_includes_gst` Check on Purchase Order (default=1, insert_after=payment_schedule, permlevel=0, no_copy=0). DB-stored `payment_amount` UNCHANGED. When unticked AND `grand_total>net_total`, every Payment Schedule row decomposes both in the live form grid AND the BBPL PO PDF: `<net> + GST <tax>` OR `<net> + Tax <tax>`. Smart label picks `+ GST` when any `doc.taxes[]` row has 'GST' in description/account_head (case-insensitive `/\bGST\b/i` in JS, plain `in` in Jinja), `+ Tax` otherwise (covers freight-only POs cleanly). Heading also flips between `(Including GST)` / `(Excluding GST)`. **NEW JS** `po_payment_amount_gst.js` (~85 LOC) loaded via **app_include_js** — switched from `doctype_js` after live-debug confirmed Frappe form-meta cache trap blocks new JS visibility (`typeof _ts_apply_gst_decomposition === 'undefined'` even after `bench build` + `clear-website-cache` + hard refresh; `app_include_js` cache-busted by bench build hash, worked first try). Smart cell selector: `.grid-static-col[data-fieldname=payment_amount]` (v15) with fallback to `[data-fieldname=...]`. Saves original cell HTML in `data-ts-gst-original` attr; restores cleanly on re-toggle. Original plan was 330 LOC / 600 tests / HIGH risk; user clarified scope to "display-only" (DB math unchanged) which collapsed to 114 LOC / 50 scenarios / LOW risk. PI carryover deferred (no BBPL PI print format exists yet). Demo verified PUR-ORD-2026-03485 (50/50 GST → form rows show `₹X + GST ₹Y` inline). Prod smoke `BBPL-PO-OT-2026-00181` → `₹6,197.80 + GST ₹1,115.60` ✓. Security 0/0/0/0/2 INFO. PO_FULL + MR_FULL temporarily unlocked for shared setup.py edit, re-locked after regression PASS. 5 files +114/-2 LOC. 80 sessions preserved. Prod regression 17/17 PASS. |
| **2.10.0.7** | 16 May | **BBPL Material Request print format — Remarks section + quotation note** — adds new conditional Remarks section rendering `doc.ts_remark` (Small Text Custom Field on MR — previously NOT shown in BBPL MR format even though the field has been populated in production). Uses existing `.bbpl-section-heading` + `.bbpl-terms-body` styling. Below it, an unconditional yellow-highlighted ⚠ Note callout: "Please mention material request no. in your quotation." — instructs suppliers when the MR is shared as a quotation request. Both inserted between existing Terms and Conditions section and Footer. Demo verified on PR-IT-26-00109 (ts_remark='TEST': Remarks + Note both shown) and BBPL-TRAN-MEC-26-00014 (ts_remark=None: Remarks hidden, Note still shown). Security 0/0/0/0/1 INFO (autoescape protects `{{ doc.ts_remark }}` — no `\|safe` filter applied). Pure print-format JSON edit. 2 files +1/-1 LOC each. 77 active sessions preserved through deploy. Prod regression 17/17 PASS. |
| **2.10.0.6** | 16 May | **BBPL Purchase Order print format — Item Remark col, Payment Description fallback, invoice-match note** — three Jinja edits to the PO format. (1) New 'Item Remark' column added between Item and Quantity, reading `item.ts_item_remark` (Data field). tfoot colspans bumped 6→7 to match the new column count. (2) Payment Terms 'Description' cell logic flipped from `{{ ps.payment_term or ps.description or '' }}` (which showed the link name first) to `{{ ps.description or (frappe.db.get_value('Payment Term', ps.payment_term, 'description') if ps.payment_term else '') or '' }}` — prefer user-entered description, fall back to Payment Term template description. (3) Unconditional yellow-highlighted ⚠ Note "All invoice entries must match the details in the purchase order." placed standalone between Grand Total and Payment Terms section. CSS polish: `white-space: nowrap` on `.num` and `.total-val` so ₹ amounts in crores fit on one line. Column widths rebalanced (S.No 30, Item Remark 85, Quantity 50, Unit 40, Rate 70, GST% 45, Amount 110px). Demo verified on PUR-ORD-2026-03485, PUR-ORD-2026-03539, prod smoke on BBPL-PO-OT-2026-00181. Security 0/0/0/0/2 INFO (autoescape + ORM-bound Payment Term lookup). 2 files. 77 sessions preserved. Prod regression 17/17 PASS. |
| **2.10.0.5** | 15 May | **P0 fix — Material Request amend save blocked by hidden `from_warehouse`** — ERPNext native validator (`BuyingController.validate_from_warehouse` at buying_controller.py:262) throws "Row #N: Accepted Warehouse and Supplier Warehouse cannot be same" when `item.from_warehouse == item.warehouse`. For non-Material-Transfer MR types (Purchase / Material Issue / Manufacture / Customer Provided Material), both row-level `from_warehouse` AND header `set_from_warehouse` are HIDDEN by `depends_on:eval:parent.material_request_type=="Material Transfer"`. During AMEND, stale values carry over invisibly; users get blocked on save with NO UI path to fix. Client (mechanical@betulbiofuel.com → escalated to hr.navaahar's amended Purchase MR `PR-HR-26-00022`) hit this live. Fix: new file `ts_gate_entry/ts_mr_warehouse_guard.py` with `mr_clear_invisible_from_warehouse(doc, method=None)` wired as `Material Request.before_validate` — when `material_request_type != "Material Transfer"`, force-clears `doc.set_from_warehouse` + every row's `from_warehouse`. Material Transfer MRs unchanged. Drop-ship safety verified (BBPL doesn't use drop-ship; non-Transfer drop-ship in ERPNext uses `Item.supplier`, not `from_warehouse`). Lesson 272 candidate: `depends_on` hides field from UI but NOT from `validate()` — auto-clear server-side OR remove the depends_on. 5 demo smoke scenarios PASS (Purchase collision→saves cleared, Transfer valid→preserved, Issue collision→cleared, Manufacture collision→cleared, Transfer legit-collision→still rejected). Prod smoke on synthesized PR-HR-26-00026 → saved cleanly with both fields auto-cleared. 3 files +35/-1 LOC. 77 sessions preserved. Prod regression 17/17 PASS. |
| **2.10.0.4** | 15 May | **BBPL Purchase Receipt print format polish** — header rewritten to use `<img src="/files/BBPL-LH.png">` letterhead image (replaces inline header table); bold wraps on Receipt No / Supplier / Source PO / Bill No values for prominence; Vehicle No + LR No now auto-fetch through the Gate Entry → Token chain (`TS Gate Entry.vehicle_number_display` / `lr_number` then `TS Token.vehicle_number` fallback) when PR's own fields are blank; Transporter row removed per user request; Source PO moved into the main info table (out of Receipt Context). Pure print-format edit — no Python / no permission / no schema change. Demo regression 17/17 BEFORE+AFTER, live-data-tester 7/7 PASS (token+chain, no-token, empty vehicle_no, cancelled, GST 18%, Exempt, Terms set). Security 0/0/0/0/2 INFO (pre-existing `\|safe` filters on privileged Terms / Item description fields, unchanged). 2 files (print format JSON + version bump). |
| **2.10.0.3** | 15 May | **PO-from-MR exemption in budget breach detector** — `_submit_po_for_approval` now bypasses `detect_budget_breach_for_po` when every PO line claiming `material_request` verifies as `docstatus=1 + ts_mr_status='Approved' + company=doc.company` (fail-closed: forged / draft / cancelled / pending / nonexistent / cross-company refs do NOT bypass, the breach check runs as normal). Standalone POs unchanged. Fixes "every PO from MR triggers Pending Budget Override" report. Demo data: 3 stale ₹6.3cr+₹65L+₹22L TTPL Test POs on Coal-BBPL cancelled (committed dropped 271% → 0%); 1 PO blocked by TS G1 link (₹15L, 8% utilization, acceptable). Forgery resistance: 7 direct-logic tests on demo PASS (Approved+same-company = bypass, all 6 others = blocked). 2 files +29/-2 LOC. Security 0/0/0 after MEDIUM fix. |
| **2.10.0.2** | 15 May | **Audit-log enum fix** — extends `TS Approval Log.action` Select options to include `Held for Budget Override` + `Resumed after Budget Override` (added via the doctype JSON `options`, picked up by `bench migrate` `sync_fields()` — no Property Setter / Lesson 239 patch needed). Replaces 2 call-site literals (`"Resumed"` → `"Resumed after Budget Override"`) at the budget-override resume paths in `_submit_*_for_approval`. Existing try/except wrappers around audit writes are KEPT as defense-in-depth per Lesson 238 — audit log is best-effort and must never block the primary workflow. Closes open item (c) from the v2.10.0 ship. Demo regression 17/17 BEFORE+AFTER, demo + prod smoke INSERT of both new action values on a real PO succeeded then cleanup. Prod regression 17/17. 3 files +4/-4 LOC. 22 active sessions preserved through deploy. |
| **2.10.0.1** | 15 May | **Best-effort wrap on budget-override audit log** — `_log_approval_action` + `_log_mr_action` calls inside `_submit_*_for_approval` budget-override branches wrapped in try/except per Lesson 238. Smoke on PUR-ORD-2026-04501 caught: action="Held for Budget Override" not in TS Approval Log Select options → ValidationError; state mutations had already committed via db_set, but exception propagated. Fix: best-effort audit row; flow no longer blocks. Override doc + tabComment carry primary audit trail. Follow-up closed by v2.10.0.2 (above). 2 files +42/-19 LOC, regression 17/17 PASS prod. |
| **2.10.0** | 15 May | **Budget-Exceeded → CEO Approval flow on MR + PO** — new submittable DocType `TS Budget Override Approval` (autoname `BO-{YYYY}-{#####}`, 48 fields, permlevel=1 tamper-wall) + child `TS Budget Override Breach Line` for multi-CC breakdowns. New status `Pending Budget Override` on `ts_mr_status` + `ts_approval_status`. Kill switch `TS Settings.ts_budget_override_flow_enabled` (default ON). Breach detector covers annual + monthly (whichever first), uses `transaction_date`-aware month. 6 whitelisted endpoints + `cancel_linked_override_on_source_cancel` hook. Linked-override banners on MR/PO forms via shared `_show_ts_banner` / `_show_ts_banner_po`. Approve/Reject/Cancel form buttons with typed-confirm on Reject (≥10-char comment + source-name match). Replaces legacy PO hard-block + manual `ceo_budget_override` (deprecated when kill switch ON; re-enables as emergency escape valve when OFF). **Bundled v2.9.17.9 FY fix:** `_resolve_fiscal_year` now date-based (finds FY where today ∈ [year_start, year_end]) + MR/PO banners show FY label. Phase 4 audits all GREEN: security 0/0/0/0/0 final, live-data-tester 30/4 (FAIL1 + FAIL2 fixed, FAIL3 deferred to v2.10.1 polish), ui-designer SHIP, guardian 14/14, predictor CRITICAL-mitigated. E2E smoke approved real demo `BO-2026-08696` → source PO transitioned `Approved → Pending CEO` with TS Approval Log action="Resumed". Regression 17/17 PASS pre+post all fixes. |
| **2.9.17.9** | 15 May | **P0 hotfix — server-side guard against MR approval-chain bypass.** Stock User role had submit=1 on Material Request; users could bypass `submit_for_approval` via native Submit button / Ctrl+Shift+Enter / `frappe.client.submit`. Three prod MRs (PR-CF-PRD-26-00008, 00008-1, 00011 by `takshak.sambare`) had been stranded docstatus=1 with `ts_mr_status='Not Submitted'`, route=NULL. Fix: new `mr_before_submit_block_direct(doc, method)` wired as `Material Request.before_submit`. State-based guard (no flag): bypass Material Transfer/Issue; bypass legit `_approve_mr` Final Approve path (route+status='Approved' set via db_set before submit); else throw. Recovery: cancelled originals, amended into `PR-CF-PRD-26-00008-2` + `PR-CF-PRD-26-00011-1` both at `Pending Department Head` step 2/3. Lessons 270 (`before_submit` fires AFTER docstatus set to 1) + 271 (Lesson 252 flush needs fresh console for smoke). |
| **2.9.17.4** | 14 May | **BBPL Purchase Receipt print format + Print PDF button** — new branded PDF print format mirroring locked PO/MR pattern (table-based layout per Lesson 232, Jinja namespace-correct loop scope, letterhead-overlay fix). Toolbar "🖨 Print PDF" button on submitted PRs (`pr_pi_columns.js` adds `_add_bbpl_print_button` calling `frappe.utils.print_format.download_pdf`). Supplier Address resolved via Address doctype lookup; "Location From PO" sourced from `ts_delivery_location`; per-item GST cell uses Item Tax Template name parsing with `doc.taxes[0].rate` fallback. Includes parse_json bug-fix from review iteration. |
| **2.9.16.7** | 13 May | TS Gate Entry DocPerm seeder for demo/prod parity — idempotent `_seed_gate_entry_docperm()` mirrors v2.9.15.2 Cost Center pattern, re-asserts 10-role DocPerm set on every migrate (Accounts Manager, CEO, G1 Security, G2 Gate Operator, IT Head, MD, Quality Inspector, Stores User, System Manager, Weighbridge Operator). Forward-safe against Lesson 169 master-data drift between environments. |
| **2.9.16.6** | 13 May | **HOTFIX** — Weighbridge "No permission for TS Settings" popup eliminated. `ts_weighbridge_log.js:45` was calling `get_single_value("TS Settings", "ts_flow_v28_enabled")` directly; Weighbridge Operator role lacks TS Settings read perm → modal on every new entry. Fix: new role-scoped `get_flow_v28_flag()` whitelist helper (Lesson 168 pattern, same as `get_g2_print_mode` / `get_two_pass_flag`). |
| **2.9.16.5** | 13 May | **P0 HOTFIX** — `ts_gate_entry/api/` package introduced in v2.9.16 silently shadowed pre-existing `ts_gate_entry/api.py` (Python: package wins over module of same name). 7 whitelisted endpoints unreachable for ~3 hours (Weighbridge token search, Weighbridge weight fetch, G2 print mode, two-pass flag, PO autocomplete, PO lifecycle, SLA scheduler). Fix: move all `api.py` content into `api/__init__.py`, delete `api.py`. New Lesson 264 captured. |
| **2.9.16.4** | 13 May | Purchase Invoice "Token / Receipt Context" section — 5 read-only fields (Token Number/Gate Pass, RST Number, Quality Inspection, Deduction Sheet, Purchase Receipt) snapshotted at insert from first linked PR via items[].purchase_receipt. DS link chain: PR.ts_token → QI (by token_number) → DS (by quality_inspection). One-time backfill on deploy populated 327 existing prod PIs. |
| **2.9.16.3** | 13 May | TS Deduction Suggestion: `purchase_order` Link Custom Field added to Receipt Context section, snapshotted at insert from source QI. Seeder also auto-bumps DocType.modified to invalidate browser form-meta cache (Lesson 263 — caught live; without the bump, newly-added Custom Fields stay invisible until users run `localStorage.clear()`) |
| **2.9.16.2** | 12 May | TS Stock Balance Computed audit-grade Script Report — wraps native, replaces stored val_rate with bal_val/bal_qty live, adds Drift % audit signal column. Stock User excluded (audit-grade). |
| **2.9.16.1** | 12 May | Stock Reports sub-workspace under BBPL Ethanol (seq=10) with 4 shortcuts: TS Stock Ledger FIFO, Stock Ledger (native), Stock Balance → TS Stock Balance Computed, Stock Entry. |
| **2.9.16** | 11 May | TS Stock Ledger FIFO custom Script Report (Cost Center via LEFT JOIN tabGL Entry + COALESCE expense-account preference per Lesson 257, Total Amount = abs(qty)×rate, branded A4-landscape PDF export with BBF logo, 5000-row hard cap). |
| **2.9.15.2** | 12 May | Cost Center read perm seeder for 7 BBF approval roles (Grain Purchase Manager, Department Head, General Manager, AVP, Grain Manager, Quality Manager, Admin Reception) — fixes "No permission for Cost Center" popup blocking PO/MR list views (standard-filter Link autocomplete required CC read) |
| **2.9.15.1** | 12 May | TS Deduction Suggestion "Receipt Context" — 5 read-only fields (Vehicle Number, RST Number, Total Net Weight kg, Supplier Code, Supplier Name) surfaced above QI section; 4 snapshot at insert from QI+PO, net_weight live-fetches from matching Weighbridge Log |
| **2.9.14.6** | 11 May | Stores Workflow submit/approve unblocked (tamper-guard flag fix in ts_mr_transfer) |
| **2.9.14.5** | 11 May | Material Request "Available Qty" shows live stock via new onload hook (ts_mr_available_refresh) |
| **2.9.14.4** | 11 May | PurchaseInvoice class override allows `update_stock=1` on Purchase Returns |
| **2.9.14.3** | 9 May | PI `update_stock` checkbox now visible on Purchase Returns (Property Setter override) |
| **2.9.14.2** | 9 May | G1 → G2 vehicle_origin always-sync (dropped over-aggressive empty-check) |
| **2.9.14.1** | 9 May | SR → PO restore MR `material_request_type` after mapping (try/finally pattern) |
| **2.9.14** | 9 May | Vehicle Origin Custom Field on G1/G2/PR/PI + Awesomplete autocomplete + propagation hooks |
| **2.9.13** | 8 May | Purchase Invoice `before_submit` validator: strict bill_no/bill_date match against linked PRs |
| **2.9.12.7** | 8 May | TS Quality Inspection `populate_template_rows` perm fix for new (unsaved) docs |
| **2.9.12.5** | 7 May | Approval Flow Health Check page (7 validators + 2 capability simulators) |

---

## Key Features

| # | Feature | Version |
|---|---------|---------|
| 1 | **Gate-Pass Workflow (Token)** — Stock IN (9-step) + Stock OUT (5-step) + Gate Pass | v1.0+ |
| 2 | **Category-Based PO Approval** — Store/Chemical/Grain/Coal routing | v2.0 |
| 3 | **Cost-Center MR Approval** — CC-based routing with Hold/Resume | v2.0+ |
| 4 | **CC Approval Config** — Per-CC indent/approval/notification with data isolation | v2.5 |
| 5 | **Budget Management** — Monthly budgets, PO blocked on exceed, MR warning banner | v2.1+ |
| 6 | **Item Creator** — 5-step wizard + bulk CSV import | v1.4 |
| 7 | **Quality & Deductions** — Grain/Coal grading with auto-calculations | v1.0 |
| 8 | **Interactive Dashboards** — CEO, MD, Gate Operations, Procurement + Wall Displays | v2.4 |
| 9 | **Gate Pass & Visitor** — Visitor tracking with G2 checkpoint, Admin Reception | v2.2 |
| 10 | **Material Inspection** — Non-RM approve/reject/hold with 4-stage SLA | v2.2 |
| 11 | **Stock OUT / Dispatch** — Reversed weighbridge, Delivery Note on exit | v2.3 |
| 12 | **Weighbridge Hardware** — Fetch weight directly from weighbridge via HTTP | v2.6 |

---

## Architecture

### 40 DocTypes

**Gate Operations (15):** TS Token, TS Gate Entry, TS Gate Entry Item, TS Gate Entry PO, TS Weighbridge Log, TS Quality Inspection, TS Deduction Sheet, TS Deduction Line, TS Unloading Entry, TS Transport Master, TS Transporter Vehicle, TS Vehicle Master, TS Driver Master, TS Gate Pass Destination, TS Visiting Company, TS Visitor

**Approval System (10):** TS Purchase Category, TS Purchase Category Item, TS PO Approval Rule, TS PO Approval Step, TS MR Approval Route, TS MR Approval Step, TS MR Route Cost Center, TS Notification Recipient, TS Approval Log, TS CC Approval Config, TS CC Approval User

**Budget (3):** TS Budget Proposal, TS Budget Proposal Item, TS Budget Override Log

**Item Management (5):** TS Item Creator, TS Item Creator Variant, TS Item Code Settings, TS Code Counter, TS Variant

**Masters (3):** TS Settings, TS Location, TS Material Inspection, TS Material Inspection Item

### 9 Custom Pages

| Page | URL |
|------|-----|
| Item Creator | `/app/item-creator` |
| Item Bulk Import | `/app/item-bulk-import` |
| Budget Control Matrix | `/app/budget-control-matrix` |
| Procurement Dashboard | `/app/procurement-dashboard` |
| Gate Operations Dashboard | `/app/gate-operations-dashboard` |
| Gate Wall Display | `/app/gate-wall-display` |
| CEO Dashboard | `/app/ceo-dashboard` |
| CEO Wall Display | `/app/ceo-wall-display` |
| MD Dashboard | `/app/md-dashboard` |

### 9 API Files

| File | Purpose |
|------|---------|
| `api.py` | PO search, token SLA, weighbridge fetch, PO lifecycle |
| `api_bulk_import.py` | CSV bulk item import |
| `ts_po_approval.py` | PO/MR approval controller, Hold/Resume, CC-aware notifications |
| `ts_budget.py` | Budget proposal, PO check, CEO override, CC budget status |
| `ts_dashboard_procurement.py` | Procurement dashboard API |
| `ts_dashboard_gate.py` | Gate operations dashboard API |
| `ts_dashboard_ceo.py` | CEO dashboard API |
| `ts_dashboard_md.py` | MD dashboard API with PIN lock |
| `dashboard_overrides.py` | PO Connections panel |

### 14 Custom Roles

G1 Security, G2 Gate Operator, Weighbridge Operator, Stores User, Quality Inspector, IT Head, Department Head, General Manager, CEO, MD, Purchase Manager, Grain Purchase Manager, AVP, Admin Reception

---

## Installation

```bash
bench get-app https://github.com/zxrrcpandey/betul_biofuel.git --branch develop
bench --site your-site install-app trustbit_ethanol
bench --site your-site migrate
```

## Deployment (Existing Site)

```bash
chown -R frappe:frappe .git
git pull upstream develop
bench build --app trustbit_ethanol
bench --site your-site migrate
bench --site your-site clear-cache
supervisorctl restart all
```

---

## Version History

| Version | Date | Changes |
|---------|------|---------|
| **2.10.0.4** | 2026-05-15 | **BBPL Purchase Receipt print format polish** — header rewrite to `<img src="/files/BBPL-LH.png">`, bold wraps on Receipt No / Supplier / Source PO / Bill No values, auto-fetch Vehicle No + LR No through Gate Entry / Token chain, Transporter row removed, Source PO promoted into main info table. Pure print-format change. 2 files. |
| **2.10.0.3** | 2026-05-15 | **PO-from-MR exemption** — `_submit_po_for_approval` bypasses `detect_budget_breach_for_po` when every claimed `item.material_request` verifies as `docstatus=1 + ts_mr_status='Approved' + same company` (fail-closed; forged/draft/cancelled/pending/nonexistent/cross-company refs all blocked). Standalone POs still check. Fixes "every PO from MR shows Budget Override" user report. 2 files +29/-2 LOC. |
| **2.10.0.2** | 2026-05-15 | Budget-override **audit-log enum fix** — `TS Approval Log.action` Select options extended (`Held for Budget Override`, `Resumed after Budget Override`); call sites use proper strings. Closes open item (c) from v2.10.0 ship. 3 files +4/-4 LOC. |
| **2.10.0.1** | 2026-05-15 | Best-effort try/except wrap on budget-override audit-log writes (Lesson 238). Workaround for missing Select options on `TS Approval Log.action` — properly fixed in v2.10.0.2. |
| **2.10.0** | 2026-05-15 | **Budget-Exceeded → CEO Approval flow** on MR + PO. New `TS Budget Override Approval` doctype + breach line child + 6 whitelisted endpoints + cancel-cascade hook. Kill switch `ts_budget_override_flow_enabled`. New status `Pending Budget Override`. Bundled v2.9.17.9 FY fix (`_resolve_fiscal_year` date-based). ~2730 LOC, 22 files. |
| **2.9.17.9** | 2026-05-15 | **P0 — server-side guard against MR approval-chain bypass.** `mr_before_submit_block_direct` hook blocks native Submit / Ctrl+Shift+Enter / API bypass. Recovery of 3 stuck prod MRs (PR-CF-PRD-26-00011 → -00011-1, 00008-1 → 00008-2, PR-PRC-BIO-26-00008 → -00008-1). 3 files +40/-1 LOC. Lessons 270 + 271. |
| **2.9.17.8** | 2026-05-14 | Item Creator Item Name **duplicate-detection typeahead** — as user types (3+ chars) in Item Name field, dropdown appears with up to 10 existing items matching via 2-stage search: Stage 1 token-LIKE (handles wrong-sequence like `rice broken` → `Broken Rice`) and Stage 2 `difflib.get_close_matches` Levenshtein fallback (handles typos like `Ricee` → `FCI Rice`). Sorted by `usage_count DESC` so most-used items rank first. Click row opens modal with item details (code/name/group/brand/UOM/usage) + "Open this item" route or "Continue — my item is different". New `@frappe.whitelist() search_existing_items()` helper (fail-closed try/except, perm gate, parameterized SQL). Full keyboard nav (Down/Up/Enter/Esc), 300ms debounce, click-outside-close, dark-mode + mobile breakpoints. 5 files, ~457 LOC. Performance: 30-45ms server-side on prod's 3258 items. |
| **2.9.17.7** | 2026-05-14 | **Material Type dropdown** Custom Field added to TS Gate Entry form. New Select field `ts_material_type` with 15 options (Store Material / Scrap / Bardana / Fly Ash / DWGS / Maize / Rice / Coal / DDGS / Ethanol / Liquid Co2 / Iron / DORB / Fusel oil / Dry Ice). Inserted after `material_flow` in flow_section tab. Installs via `create_custom_fields()` on `after_migrate` hook. No JS / no Python logic / no permission change. |
| **2.9.17.6** | 2026-05-14 | Item Creator default Stock UOM flipped `Kg → Nos`. Value-only change (5 occurrences in `item_creator.js`). Validated against prod data: 3010 items use Nos vs 248 use Kg (92% Nos). Operators no longer need to re-select Nos on every new item. Kg remains selectable. |
| **2.9.17.5** | 2026-05-14 | Item Creator (`/app/item-creator`) flat-form rewrite — 5-step wizard collapsed into single scrolling page with 5 collapsible `<details>` accordion sections + sticky live-code preview bar. Bundled audit fixes: Lesson 168 whitelisted `get_variant_code()` helper (fail-closed), Lesson 163 rogue `__init__.py` removal, Lesson 174 version badge `v2.9.17.5`, dark-mode contract (17 `[data-theme="dark"]` overrides), mobile breakpoints (768px + 480px), accessibility (native `<details>` keyboard nav, `aria-live` on sticky code, `role=radio/tab`, `:focus-visible`). Net −285 LOC. 6 files. |
| **2.9.17.4** | 2026-05-14 | BBPL Purchase Receipt branded Print Format + 🖨 Print PDF button on PR form toolbar. Print format mirrors BBPL PO/MR layout with Supplier Invoice No/Date, Vehicle, Warehouse, Cost Center, Project, optional Supplier Address (multi-line via `frappe.get_doc('Address', ...)`), Location (from items[0].ts_delivery_location), conditional Receipt Context (Token/GE/RST/WB Net/QI/DS/Source PO/QC Status), items table with per-item GST cell (amount bold + rate label), totals + Amount in Words + Terms + Received/Verified signatures + BBPL footer. |
| **2.9.16.7** | 2026-05-13 | TS Gate Entry DocPerm seeder for demo/prod parity — fixed silent permission gap when Custom DocPerm overrides Standard DocPerm; added `_seed_gate_entry_docperm()` idempotent seeder (10 roles incl. Weighbridge Operator). Lesson 169 pattern. |
| **2.9.16** | 2026-05-13 | Stock Reports module (TS Stock Ledger FIFO + TS Stock Balance Computed), DSG PO link, Quality Lab dashboard, PI Receipt Context (5 fields with backfill on 327 PIs). |
| **2.9.15.1** | 2026-05-12 | TS Deduction Suggestion Receipt Context — 5 fields above QI section (vehicle, RST, supplier code/name snapshots + live Net Weight via whitelisted `get_live_net_weight`). |
| **2.9.x** | 2026-04 to 2026-05 | Stores Receiving Dashboard with token-based GRN, Stock Workflow MR transfers, Quality Inspection refinements, Purchase Invoice Receipt Context, MR Available Qty live refresh, PI/PR column updates, MR/PO Print PDF buttons, RST capture on Weighbridge, two-pass G2 gate flow. |
| **2.6.1** | 2026-04-01 | G1 driver details (name/license/mobile) + G2 auto-fetch, operator name, vehicle/driver master removed from G2 |
| **2.6.0** | 2026-03-30 | Complete BBF → TS rename (40 DocTypes, 50+ fields), dual logo (client+Trustbit), weighbridge hardware integration, project on MR/PO, item remark, define use location on MR/PO/PR/PI |
| **2.5.0** | 2026-03-25 | CC Approval Config, Hold/Resume, CC-aware notifications, monthly budgets, stock availability, approval status in list views |
| **2.4.0** | 2026-03-23 | CEO/MD/Gate/Procurement dashboards + wall displays, PO lifecycle tracker |
| **2.3.0** | 2026-03-22 | Stock OUT dispatch, G1/G2 field split, multi-PO gate entry, vehicle/driver blacklist |
| **2.2.x** | 2026-03-19 | Gate Pass, material inspection, optional weighbridge for Non-RM, admin reception |
| **2.1.x** | 2026-03-17 | Budget management module |
| **2.0.x** | 2026-03-17 | Complete approval redesign + 45 bug fixes |
| **1.4.x** | 2026-03-12 | Item Creator, Bulk Import |
| **1.0** | 2026-03-08 | 9-step gate entry flow |

---

## License

MIT

---

*Developed by [Trustbit Technologies Pvt. Ltd.](https://trustbit.com) for Betul Bio Fuel Pvt. Ltd.*
